// ABOUTME: The vendored Steel skill catalog, generated by scripts/sync-skills.mjs from
// ABOUTME: steel-dev/skills@35bf278371312964a177f15b679b5e290f9ef275. Markdown guidance only; edit upstream, then re-run the sync.
export const SKILL_CATALOG_REF = "35bf278371312964a177f15b679b5e290f9ef275";

/** One markdown file of one skill, at its upstream-relative path. */
export interface CatalogFile {
    readonly path: string;
    readonly text: string;
}

export const SKILL_CATALOG: readonly CatalogFile[] = [
    { path: "steel-browser/README.md", text: "# Steel Browser Skill\n\nSkill for agent-driven web workflows using Steel cloud browsers and API tools.\n\n## Install\n\nFrom the public Steel skills catalog:\n\n```bash\nnpx skills add steel-dev/skills --skill steel-browser\n```\n\nFrom local checkout:\n\n```bash\nnpx skills add ./skills/steel-browser\n```\n\nWith the Steel CLI helper:\n\n```bash\nsteel skills install steel-browser\n```\n\n## When to use this skill\n\nUse this skill by default for browser/web tasks that do not require local-only\nexecution:\n\n- Multi-step website automation (`steel browser ...`)\n- Web extraction/summarization (`steel scrape`)\n- Page artifacts (`steel screenshot`, `steel pdf`)\n- Anti-bot-sensitive flows that benefit from session continuity, CAPTCHA tools,\n  and proxy support\n\nPrefer Steel tooling over generic fetch/search paths when reliability matters.\nSkip only for local QA workflows (for example `localhost`) or private-network\ntargets that cannot run in Steel cloud sessions.\n\n## Why Steel\n\n- Cloud background browser sessions with explicit lifecycle control\n- Free-plan-friendly defaults (minimal fingerprinting, up to 2 concurrent\n  browsers, and 100 browser hours)\n- API tools that often outperform fragile one-off scraping attempts\n- Compatibility with `agent-browser` command patterns plus Steel lifecycle\n  commands\n\n## Quick start patterns\n\n### Extraction first\n\n```bash\nsteel scrape https://example.com --format markdown\n```\n\n### Interactive session\n\n```bash\nSESSION=\"job-$(date +%s)\"\nsteel browser start --session \"$SESSION\"\nsteel browser open https://example.com --session \"$SESSION\"\nsteel browser snapshot -i --session \"$SESSION\"\nsteel browser stop --session \"$SESSION\"\n```\n\n## Troubleshooting (abbreviated)\n\nRun first:\n\n```bash\nsteel browser sessions\nsteel browser live\n```\n\nQuick fixes:\n\n- Auth errors: run `steel login` or set `STEEL_API_KEY`\n- Session churn: reuse the same `--session <name>` across every command\n- CAPTCHA blocks: check `steel browser captcha status --wait`; use\n  `steel browser captcha solve --session <name>` when in manual mode\n- Local/self-hosted failures: verify `--local` / `--api-url`, then start local\n  runtime (`steel dev install`, `steel dev start`)\n\nUse the full troubleshooting guide for deeper recovery playbooks. Use\n`steel-session-debugging` for failed session diagnosis and `steel-reliability`\nfor bot-detection, proxy, CAPTCHA, identity, or login reliability issues.\n\n## Contents\n\n- `SKILL.md`: trigger and workflow instructions.\n- `references/`: lifecycle, commands, migration, and troubleshooting guides.\n- `evals/evals.json`: routing and command-behavior assertions.\n" },
    { path: "steel-browser/SKILL.md", text: "---\nname: steel-browser\nallowed-tools:\n  - \"Bash(steel:*)\"\ndescription: 'Use this skill for live web tasks where WebFetch or curl would fail or be insufficient: JavaScript-rendered pages, forms, screenshots, PDFs, login flows, CAPTCHA/bot-protection flows, and multi-step navigation. It runs a real Steel cloud browser through the Steel CLI. Use when the agent should visit, click, type, scrape, screenshot, or extract now. Do not use for writing reusable Steel SDK/API code; use steel-developer for code. If a live task fails, hand off to steel-session-debugging; if evidence points to bot detection, proxies, CAPTCHA, or identity, hand off to steel-reliability.'\nlicense: MIT\ncompatibility: claude-code,codex,cursor,opencode,pi\nmetadata:\n  owner: steel\n  category: operate\n  stage: ga\n---\n\n# Steel Browser\n\nUse Steel CLI browser tools to complete live web work now.\n\n## Setup\n\nIf Steel is missing or unauthenticated:\n\n```bash\nsteel --version\nsteel login\nsteel doctor --preflight\n```\n\nIf the CLI or installed skills look broken, run:\n\n```bash\nsteel skills doctor\n```\n\nDone when this smoke test succeeds:\n\n```bash\nsteel scrape https://example.com\n```\n\n## Choose The Right Path\n\n- Static page content: `steel scrape <url>`\n- Screenshot: `steel screenshot <url>`\n- PDF: `steel pdf <url>`\n- Multi-step interaction, login, forms, or JS-heavy pages: `steel browser ...`\n- Bot detection, CAPTCHA, proxy, or login reliability failure: collect evidence with `steel-session-debugging`, then use `steel-reliability`\n- Reusable application code: use `steel-developer`\n\nStart with the smallest tool that can finish the task. Escalate from scrape/screenshot/PDF to an interactive browser session only when interaction or state is required.\n\n## Interactive Session Workflow\n\n```bash\nsteel browser start --session my-task --session-timeout 3600000\nsteel browser navigate https://example.com --session my-task\nsteel browser snapshot -i --session my-task\nsteel browser click @e1 --session my-task\nsteel browser wait --load networkidle --session my-task\nsteel browser snapshot -i --session my-task\nsteel browser stop --session my-task\n```\n\nRules:\n\n- Always use the same `--session <name>` on every command.\n- Take a fresh `snapshot -i` after navigation or DOM changes because element refs expire.\n- Prefer `@eN` refs from snapshots over brittle CSS selectors.\n- Use `steel browser batch` when a sequence needs an action and immediate re-snapshot.\n- Release/stop sessions unless the user explicitly asks to keep them open.\n\n## Troubleshooting\n\n1. Run `steel skills doctor` for install or CLI-skill mismatch issues.\n2. Run `steel doctor --preflight` for auth/API issues.\n3. Use `steel browser sessions` and `steel browser live --session <name>` for active sessions.\n4. For failed completed sessions, load `steel-session-debugging`.\n5. For bot/proxy/CAPTCHA/identity fixes, load `steel-reliability`.\n\n## References\n\n- `references/steel-browser-commands.md`: command reference.\n- `references/steel-browser-lifecycle.md`: session lifecycle discipline.\n- `references/troubleshooting.md`: recovery playbooks.\n- `references/migration-agent-browser.md`: migration from upstream `agent-browser`.\n" },
    { path: "steel-browser/references/migration-agent-browser.md", text: "# Migration from `agent-browser` to `steel browser`\n\nUse this reference when users bring existing upstream scripts or habits.\n\n## Core migration rule\n\nReplace the command prefix:\n\n- Before: `agent-browser <command> ...`\n- After: `steel browser <command> ...`\n\nSteel keeps the same CLI interface as agent-browser and adds lifecycle controls.\n\n## Differences\n\nMost commands work with a direct prefix swap. These are the exceptions:\n\n| agent-browser           | steel browser              | reason              |\n| ----------------------- | -------------------------- | ------------------- |\n| `tab 2`                 | `tab switch 2`             | positional → subcmd |\n| `wait @e1`              | `wait --selector @e1`      | positional → flag   |\n| `wait 2000`             | `wait --timeout 2000`      | positional → flag   |\n| `screenshot ./page.png` | `screenshot -o ./page.png` | positional → flag   |\n\n## Example conversion\n\n```bash\n# Before\nagent-browser open https://example.com\nagent-browser snapshot -i\nagent-browser click @e3\nagent-browser get text @e7\nagent-browser wait --load networkidle\nagent-browser screenshot ./page.png\n\n# After — just swap the prefix (aliases handle the rest)\nsteel browser start\nsteel browser open https://example.com\nsteel browser snapshot -i\nsteel browser click @e3\nsteel browser get text @e7\nsteel browser wait --load networkidle\nsteel browser screenshot -o ./page.png\nsteel browser stop\n```\n\n## Steel-only commands\n\nThese are Steel additions not present in agent-browser:\n\n- `steel browser start` — create/attach a cloud browser session\n- `steel browser stop` — release the session\n- `steel browser sessions` — list active sessions\n- `steel browser live` — open the live session viewer\n- `steel browser captcha status/solve` — CAPTCHA management\n\nUse `steel browser --help` to see all currently available commands.\n" },
    { path: "steel-browser/references/steel-browser-commands.md", text: "# Steel Browser Commands Reference\n\nUse this reference when you need command-level patterns for navigation, interaction, extraction, and debugging.\n\nFor any command, run `steel browser <command> --help` for full flag details.\n\n## Navigation\n\n```bash\nsteel browser open <url>\nsteel browser navigate <url> --wait-until networkidle\nsteel browser navigate <url> --header \"Authorization: Bearer ...\"\nsteel browser back\nsteel browser forward\nsteel browser reload\nsteel browser close\n```\n\n`open` is an alias for `navigate`.\n\n## Snapshot and element discovery\n\n```bash\nsteel browser snapshot                      # all elements (default)\nsteel browser snapshot -i                   # interactive elements only\nsteel browser snapshot -c                   # compact output\nsteel browser snapshot -d 3                 # limit tree depth\nsteel browser snapshot -s \"#main\"           # scope to subtree\nsteel browser snapshot -C                   # include cursor position\nsteel browser snapshot -i -c -d 3           # combine short flags\n```\n\nSnapshot returns an accessibility tree with element refs (`@e1`, `@e2`, ...).\nUse these refs in subsequent commands instead of CSS selectors.\n\n## Interactions with element refs\n\n```bash\nsteel browser click @e1\nsteel browser click @e1 --new-tab\nsteel browser click @e1 --button right\nsteel browser click @e1 --count 2\nsteel browser dblclick @e1\nsteel browser focus @e1\nsteel browser fill @e2 \"text\"\nsteel browser type @e2 \"text\"\nsteel browser type @e2 \"text\" --clear\nsteel browser type @e2 \"text\" --delay 50\nsteel browser press Enter\nsteel browser press Control+a\nsteel browser hover @e1\nsteel browser check @e1\nsteel browser uncheck @e1\nsteel browser select @e1 \"value\"\nsteel browser select @e1 \"a\" \"b\"\nsteel browser clear @e1\nsteel browser selectall @e1\nsteel browser scroll down 500\nsteel browser scroll up\nsteel browser scroll down 200 --selector \".panel\"\nsteel browser scrollintoview @e1\nsteel browser setvalue @e1 \"raw value\"\n```\n\n`setvalue` sets the DOM value property directly without triggering input events.\nUse `fill` or `type` for normal form interaction.\n\n## Information extraction\n\n```bash\nsteel browser get text @e1\nsteel browser get html @e1\nsteel browser get value @e1\nsteel browser get attr @e1 href\nsteel browser get title\nsteel browser get url\nsteel browser get count \".item\"\nsteel browser get box @e1\nsteel browser get styles @e1\nsteel browser get styles @e1 --property color --property font-size\n```\n\nThere is no top-level `steel browser extract` command.\nUse `steel browser get ...`, `steel browser snapshot`, and `steel browser find ...`.\n\n## State checks\n\n```bash\nsteel browser is visible @e1\nsteel browser is enabled @e1\nsteel browser is checked @e1\n```\n\n## Find elements\n\n```bash\nsteel browser find \".item\"\nsteel browser find \"a[href*=login]\"\n```\n\nReturns a list of matching elements with index, tag name, text, and visibility.\n\n## Waiting and synchronization\n\n```bash\nsteel browser wait -t \"Success\"\nsteel browser wait --selector \".loaded\"\nsteel browser wait --selector \".modal\" --state hidden\nsteel browser wait -u \"/dashboard\"\nsteel browser wait --fn \"window.ready\"\nsteel browser wait --load networkidle\nsteel browser wait --timeout 5000 -t \"Done\"\n```\n\nShort flags: `-t` text, `-u` url, `-f` function.\nLong aliases: `--fn` = `--function`, `--load` = `--load-state`.\nAll wait conditions default to 30 second timeout. Override with `--timeout <ms>`.\n\n## Screenshots\n\n```bash\nsteel browser screenshot\nsteel browser screenshot -o ./page.png\nsteel browser screenshot --full\nsteel browser screenshot --selector \"#chart\"\nsteel browser screenshot --annotate\nsteel browser screenshot --format jpeg --quality 80\n```\n\n## Drag and drop\n\n```bash\nsteel browser drag @e1 @e2\n```\n\n## File upload\n\n```bash\nsteel browser upload @e1 file.pdf\nsteel browser upload @e1 file1.pdf file2.txt\n```\n\n## Highlight\n\n```bash\nsteel browser highlight @e1\n```\n\nVisually highlights the element in the browser viewport.\n\n## Cookies and storage\n\n```bash\nsteel browser cookies                            # list all cookies\nsteel browser cookies set <name> <value>\nsteel browser cookies set <name> <value> --domain .example.com --path / --secure --http-only\nsteel browser cookies clear\n\nsteel browser storage local                      # get all localStorage\nsteel browser storage local <key>                # get specific key\nsteel browser storage local set <key> <value>    # set value\nsteel browser storage local clear                # clear all\nsteel browser storage session                    # sessionStorage (same subcommands)\nsteel browser storage session <key>\nsteel browser storage session set <key> <value>\nsteel browser storage session clear\n```\n\n## Browser settings\n\n```bash\nsteel browser set viewport 1920 1080\nsteel browser set viewport 375 812 --mobile --scale 3\nsteel browser set geo 37.7749 -122.4194\nsteel browser set geo 37.7749 -122.4194 --accuracy 100\nsteel browser set offline on\nsteel browser set offline off\nsteel browser set headers '{\"X-Custom\":\"value\",\"Authorization\":\"Bearer tok\"}'\nsteel browser set useragent \"Mozilla/5.0 Custom Agent\"\n```\n\n`set geo` accepts alias `set geolocation`. `set useragent` accepts alias `set ua`.\n\n## Page content\n\n```bash\nsteel browser content\nsteel browser eval \"document.title\"\nsteel browser eval \"document.querySelectorAll('a').length\"\n```\n\n## Tabs\n\n```bash\nsteel browser tab list\nsteel browser tab new\nsteel browser tab new https://example.com\nsteel browser tab switch 2\nsteel browser tab close\nsteel browser tab close 2\n```\n\n## Window\n\n```bash\nsteel browser bringtofront\n```\n\n## Session lifecycle\n\n```bash\nsteel browser start\nsteel browser start --session <name>\nsteel browser sessions\nsteel browser live\nsteel browser stop\nsteel browser stop --all\n```\n\n## CAPTCHA control\n\n```bash\nsteel browser start --session <name> --stealth\nsteel browser captcha status\nsteel browser captcha status --wait\nsteel browser captcha status --wait --timeout 120000\nsteel browser captcha solve --session <name>\n```\n\n## Quick help\n\n```bash\nsteel browser --help\nsteel browser <command> --help\nsteel browser get --help\nsteel browser is --help\nsteel browser tab --help\n```\n" },
    { path: "steel-browser/references/steel-browser-lifecycle.md", text: "# Steel Browser Lifecycle and Modes\n\nUse this reference when planning session lifecycle, mode selection, and endpoint behavior.\n\n## Command ownership\n\nSteel-owned lifecycle commands:\n\n- `steel browser start`\n- `steel browser stop`\n- `steel browser sessions`\n- `steel browser live`\n- `steel browser captcha solve`\n\nAll other `steel browser <command>` operations are inherited passthrough behavior backed by the vendored `agent-browser` runtime.\n\n## Mode selection\n\n- Cloud mode is default.\n- Self-hosted mode is used when `--local` or `--api-url <url>` is provided.\n- Keep one mode for an entire workflow unless the user explicitly asks to switch.\n\n## Endpoint precedence\n\nSelf-hosted endpoint precedence:\n\n1. `--api-url <url>`\n2. `STEEL_BROWSER_API_URL`\n3. `STEEL_LOCAL_API_URL`\n4. `browser.apiUrl` in `~/.config/steel/config.json`\n5. `http://localhost:3000/v1`\n\nCloud endpoint precedence:\n\n1. `STEEL_API_URL`\n2. `https://api.steel.dev/v1`\n\n## `steel browser start` contract\n\nPurpose: create or attach a session.\n\nMain flags:\n\n- `--local`\n- `--api-url <url>`\n- `--session <name>`\n- `--stealth`\n- `--proxy <url>`\n- `--session-solve-captcha`\n- `--namespace <name>` — credential namespace for the session\n- `--credentials` — enable credential injection for the session\n- `--profile <name>` — load a named browser profile into the session\n- `--update-profile` — save session state back to the profile on end\n- `--inactivity-timeout <ms>` — release the session after this much idle time (no CDP command or remote input). Defaults to 120000 (2 min); pass `0` to disable.\n\nNote: sessions created via the CLI auto-release after ~2 minutes of inactivity by default. If you expect a session to sit idle between manual steps, pass `--inactivity-timeout 0` (disable) or a larger value at `start`, and check `inactivity_timeout` in the start output.\n\nParse these output fields:\n\n- `id`: stable session identifier\n- `mode`: execution mode\n- `name`: session alias if provided\n- `live_url`: live-view URL when available\n- `connect_url`: display-safe URL with sensitive values redacted\n- `inactivity_timeout`: idle-release limit when set (human output); `inactivityTimeoutMs` in JSON\n\nUse `id` for stable machine parsing. Treat `connect_url` as display metadata, not a raw credential.\n\n## CAPTCHA mode mapping (`/v1/sessions` fields)\n\nUse these creation-time configurations:\n\n- Off (default): no captcha flags.\n  API fields: `solveCaptcha = false` and no `stealthConfig.autoCaptchaSolving`.\n- Manual: `steel browser start --session <name> --session-solve-captcha`.\n  API fields: `solveCaptcha = true`, `stealthConfig.autoCaptchaSolving = false`.\n- Auto: `steel browser start --session <name> --stealth`.\n  API fields: `solveCaptcha = true`, `stealthConfig.autoCaptchaSolving = true`.\n\nManual solve command:\n\n```bash\nsteel browser captcha solve --session <name>\n```\n\nOptional targeting for a specific detected challenge:\n\n```bash\nsteel browser captcha solve --session <name> --page-id <id> --task-id <id>\n```\n\nWhen auto solving is enabled, wait for solving to finish and use screenshots to monitor progress:\n\n```bash\nsteel browser screenshot /tmp/captcha-check.png --session <name>\n```\n\n## `steel browser stop` contract\n\nPurpose: stop active sessions.\n\nMain flags:\n\n- `--session <name>`\n- `--all`\n- `--local`\n- `--api-url <url>`\n\n## `steel browser sessions` contract\n\nPurpose: list session metadata as JSON.\n\nMain flags:\n\n- `--local`\n- `--api-url <url>`\n- `--raw`\n\n`connectUrl` values are display-safe and redact sensitive query values.\n\n## `steel browser captcha solve` contract\n\nPurpose: manually trigger CAPTCHA solving for an active or named session.\n\nMain flags:\n\n- `--session-id <id>`\n- `--session <name>`\n- `--page-id <id>`\n- `--url <url>`\n- `--task-id <id>`\n- `--local`\n- `--api-url <url>`\n- `--raw`\n\n## `steel browser live` contract\n\nPurpose: print the active session live-view URL.\n\nMain flags:\n\n- `--session <name>`\n- `--local`\n- `--api-url <url>`\n\n## Profile persistence\n\n```bash\nsteel browser start --session \"$SESSION\" --profile myapp --update-profile\n```\n\n- `--profile <name>`: Load a previously imported browser profile (cookies, storage, etc.) into the session.\n- `--update-profile`: Save session state back to the profile when the session ends.\n\nProfiles are created outside agent workflows via `steel profile import`. Agents should only consume them with the flags above.\n\n## Passthrough bootstrap behavior\n\nFor inherited commands, Steel may inject resolved `--cdp` automatically.\n\n- If command already includes `--cdp`: passthrough unchanged.\n- If command includes `--auto-connect`: passthrough unchanged.\n- If both are present: fail fast.\n\n## Recommended lifecycle pattern\n\n```bash\nSESSION=\"job-$(date +%s)\"\nsteel browser start --session \"$SESSION\"\nsteel browser open https://example.com --session \"$SESSION\"\nsteel browser snapshot -i --session \"$SESSION\"\nsteel browser stop --session \"$SESSION\"\n```\n\nUse a stable session name to avoid accidental session churn across multi-step workflows.\n" },
    { path: "steel-browser/references/troubleshooting.md", text: "# Steel Browser Troubleshooting\n\nUse this reference when automation fails or behaves inconsistently.\n\n## First diagnostic pass\n\nRun:\n\n```bash\nsteel browser sessions\nsteel browser live\n# or: steel browser live --session <name>\n```\n\nThen verify:\n\n- same mode across commands (cloud vs local/self-hosted),\n- same `--session <name>` across workflow steps,\n- auth and endpoint inputs are present.\n\n## Common failures and fixes\n\n### Missing auth\n\nSymptom:\n\n- `Missing browser auth. Run steel login or set STEEL_API_KEY.`\n\nFix:\n\n```bash\nsteel login\n```\n\nor set `STEEL_API_KEY` in runtime environment.\n\n### Self-hosted API unreachable\n\nSymptom:\n\n- Errors reaching Steel session API in local/self-hosted mode.\n\nFix:\n\n1. Check endpoint flags/env resolution.\n2. For local runtime flow:\n\n```bash\nsteel dev install\nsteel dev start\nsteel browser start --local --session local-debug\n```\n\n### Session not reused\n\nSymptom:\n\n- New browser appears unexpectedly between steps.\n\nFix:\n\n- Ensure exact same `--session <name>` value on every command.\n- Ensure commands do not mix cloud and local mode.\n\n### CAPTCHA encountered mid-flow\n\nImportant constraints:\n\n- CAPTCHA solving requires a paid Steel plan.\n- Proxies are strongly recommended for CAPTCHA and anti-bot evasion.\n- `--proxy <url>` + CAPTCHA solving is the strongest default combo.\n\n**Checking CAPTCHA solving progress:**\n\n```bash\n# Quick status check (shows type if solving)\nsteel browser captcha status\n# Output: solving recaptchaV2\n\n# Wait for CAPTCHA to resolve (blocks until done)\nsteel browser captcha status --wait\n# Output: solved\n\n# With custom timeout (2 minutes)\nsteel browser captcha status --wait --timeout 120000\n\n# Full JSON for debugging\nsteel browser captcha status --raw\n```\n\nUse status checks to:\n\n- Verify CAPTCHA was detected\n- Wait for auto-solve to complete before continuing\n- Diagnose failed solves (see type in output)\n\nIf session is auto solving (`--stealth`):\n\n1. Check status and wait for resolution:\n\n```bash\nsteel browser captcha status --wait\n```\n\n2. Or capture screenshots while waiting:\n\n```bash\nsteel browser screenshot /tmp/captcha-auto-progress.png --session <name>\n```\n\nIf session is manual solving (`--session-solve-captcha`):\n\n1. Trigger solve:\n\n```bash\nsteel browser captcha solve --session <name>\n```\n\n2. Wait for completion:\n\n```bash\nsteel browser captcha status --wait\n```\n\n3. If needed, target a specific task:\n\n```bash\nsteel browser captcha solve --session <name> --page-id <id> --task-id <id>\n```\n\nIf session has no CAPTCHA solving enabled (default):\n\n1. Start a new session with solving enabled.\n2. Re-open the blocked page and continue.\n\n```bash\nSESSION=\"captcha-recovery-$(date +%s)\"\nsteel browser start --session \"$SESSION\" --session-solve-captcha --proxy http://user:pass@host:port\nsteel browser open <blocked-url> --session \"$SESSION\"\n```\n\n### No active live session\n\nSymptom:\n\n- `No active live session found.`\n\nFix:\n\n```bash\nsteel browser start --session my-job\nsteel browser live\n```\n\n### Stale state or stuck mapping\n\nFix:\n\n```bash\nsteel browser stop --all\nsteel browser start --session fresh-job\n```\n\n## Safe CDP handling\n\n- Treat `connect_url`/`connectUrl` as display-safe only.\n- Build auth-bearing URLs from `session id` plus environment key in runtime, not from copied log output.\n\n## Recovery pattern for agents\n\n1. Capture failing command and exact error text.\n2. Identify whether issue is auth, mode, endpoint, or session continuity.\n3. Apply the smallest fix that addresses root cause.\n4. Re-run prior command before attempting additional workflow steps.\n" },
    { path: "steel-developer/README.md", text: "# Steel Developer Skill\n\nSkill for building reusable software on Steel cloud browsers with the Steel SDK, REST APIs, Playwright, Puppeteer, Stagehand, Browser Use, credentials, profiles, proxies, and CAPTCHA APIs.\n\n## Install\n\nFrom the public Steel skills catalog:\n\n```bash\nnpx skills add steel-dev/skills --skill steel-developer\n```\n\nWith the Steel CLI helper:\n\n```bash\nsteel skills install steel-developer\n```\n\n## When to use this skill\n\nUse this skill when writing, debugging, or explaining application code, scripts, reusable workflows, examples, or docs that run on Steel.\n\nUse `steel-browser` instead when the agent should operate a live browser itself via `steel scrape`, `steel screenshot`, `steel pdf`, or `steel browser ...`.\n\n## Contents\n\n- `SKILL.md`: routing, non-negotiables, and workflow guidance.\n- `references/cli-setup.md`: CLI auth, doctor, and smoke-test guidance.\n- `references/apis.md`: first-party API routing, source-of-truth links, docs-fetching guidance, and API gotchas.\n- `references/ecosystem.md`: Steel ecosystem routing for automation libraries, agent frameworks, computer-use integrations, and coding-agent integrations.\n- `references/typescript.md`: TypeScript, JavaScript, Playwright, Puppeteer, and Stagehand examples.\n- `references/python.md`: Python, Playwright Python, and Browser Use examples.\n- `references/anti-patterns.md`: stale patterns the agent must avoid.\n- `references/computer-use.md`: computer-use integration guidance.\n- `references/live-embed.md`: live and past session embed guidance.\n- `references/reliability-handoff.md`: when to load `steel-reliability` or `steel-session-debugging`.\n- `evals/evals.json`: eval prompts and assertions for routing, API coverage, and non-negotiable implementation patterns.\n\nThe eval file uses `assertions` because `agent-skills-eval` grades that key. Older local fixtures may use `expectations`, but those are ignored by this runner.\n" },
    { path: "steel-developer/SKILL.md", text: "---\nname: steel-developer\ndescription: Use this skill when the user wants reusable code, scripts, examples, or docs that build on Steel cloud browsers with SDKs, REST APIs, Playwright, Puppeteer, Stagehand, Browser Use, credentials, profiles, files, extensions, embeds, proxies, or CAPTCHA APIs. Do not use for live web browsing performed by the agent; use steel-browser. Route failed-session diagnosis to steel-session-debugging and reliability mitigation to steel-reliability.\nlicense: MIT\ncompatibility: claude-code,codex,cursor,opencode,pi\nmetadata:\n  owner: steel\n  category: build\n  stage: ga\n---\n\n# Steel Developer\n\n## Skill Boundary\n\n- Use this skill when the user wants code or implementation guidance they will run later.\n- Do not use this skill when the user wants the agent to browse, extract, screenshot, create a PDF, or fill a form now; use `steel-browser`.\n- Use `steel-session-debugging` when the main task is diagnosing a failed session.\n- Use `steel-reliability` when the main task is bot detection, proxy, CAPTCHA, identity, pacing, or login reliability mitigation.\n\n## Route By Need\n\nLoad the relevant reference before writing code:\n\n- CLI setup and local verification: `references/cli-setup.md`\n- TypeScript, JavaScript, Node.js, Playwright, Puppeteer, Stagehand: `references/typescript.md`\n- Python, Playwright Python, Browser Use: `references/python.md`\n- First-party APIs, exact docs/API/SDK lookup, browser tools, files, extensions, auth context, embeds, traces, mobile mode, multi-region: `references/apis.md`\n- Framework choice, Stagehand, Browser Use, typed agent frameworks, coding-agent integrations: `references/ecosystem.md`\n- Stale SDK/API patterns to avoid: `references/anti-patterns.md`\n- Computer-use loops: `references/computer-use.md`\n- Live and past session embeds: `references/live-embed.md`\n- Reliability boundary and handoff: `references/reliability-handoff.md`\n\n## Non-Negotiables\n\n- Auth env var is `STEEL_API_KEY`.\n- Node examples import `Steel` from `steel-sdk`; SDK source is `steel-dev/steel-node`.\n- Node SDK constructor uses `steelAPIKey`.\n- Python SDK constructor uses `steel_api_key`.\n- Always release sessions in cleanup.\n- Construct the CDP URL explicitly as `wss://connect.steel.dev?apiKey=...&sessionId=...`.\n- Reuse the default browser context after connecting; do not create a new context unless the user explicitly needs isolation.\n- Keep target-site credentials out of prompts, code comments, page scripts, and logs.\n- For exact API fields or SDK method signatures, inspect the API reference, SDK docs, local installed types, or Steel docs via `curl -sSfL`.\n- Never use stale patterns listed in `references/anti-patterns.md`.\n\n## Minimal Session Shape\n\nTypeScript:\n\n```ts\nconst steelAPIKey = process.env.STEEL_API_KEY;\nif (!steelAPIKey) throw new Error(\"STEEL_API_KEY is required\");\n\nconst client = new Steel({ steelAPIKey });\nconst session = await client.sessions.create();\nconst cdpUrl = `wss://connect.steel.dev?apiKey=${steelAPIKey}&sessionId=${session.id}`;\nconst browser = await chromium.connectOverCDP(cdpUrl);\nconst context = browser.contexts()[0];\nconst page = context.pages()[0] ?? (await context.newPage());\n\ntry {\n  await page.goto(\"https://example.com\");\n} finally {\n  await browser.close();\n  await client.sessions.release(session.id);\n}\n```\n\nPython:\n\n```python\nsteel_api_key = os.environ[\"STEEL_API_KEY\"]\nclient = Steel(steel_api_key=steel_api_key)\nsession = client.sessions.create()\ncdp_url = f\"wss://connect.steel.dev?apiKey={steel_api_key}&sessionId={session.id}\"\nbrowser = await playwright.chromium.connect_over_cdp(cdp_url)\ncontext = browser.contexts[0]\npage = context.pages[0] if context.pages else await context.new_page()\ntry:\n    await page.goto(\"https://example.com\")\nfinally:\n    await browser.close()\n    await playwright.stop()\n    client.sessions.release(session.id)\n```\n\n## Setup Verification\n\n```bash\nsteel --version\nsteel login\nsteel doctor --preflight\nsteel skills doctor\n```\n\n## Source-Of-Truth Links\n\n- API reference: https://steel.apidocumentation.com/api-reference\n- Node SDK/types: https://github.com/steel-dev/steel-node\n- Python SDK/types: https://github.com/steel-dev/steel-python\n- Docs index: `curl -sSfL https://docs.steel.dev/llms.txt`\n- Full docs bundle: `curl -sSfL https://docs.steel.dev/llms-full.txt`\n- Single docs page: `curl -sSfL https://docs.steel.dev/llms.mdx/<page-path>`\n" },
    { path: "steel-developer/references/anti-patterns.md", text: "# Anti-Patterns\n\nDo not generate these stale or risky patterns:\n\n- `steelClient`\n- `client.createSession`\n- `client.releaseSession`\n- `Steel(api_key=...)`\n- `session.websocketUrl`\n- `session.websocket_url`\n- Playwright `chromium.connect({ wsEndpoint })`\n- top-level `browser.newPage()` for a normal Steel session\n- `browser.newContext()` unless the user explicitly needs isolation\n- hardcoded API keys, usernames, passwords, TOTP secrets, proxy URLs, or credential placeholders\n- creating a session for one-shot scrape/screenshot/PDF when Browser Tools are enough\n- using `all_ext` unless the user explicitly asks to load all extensions\n- enabling Steel-managed proxy or CAPTCHA features before plan access is checked\n\nPrefer:\n\n- `import Steel from \"steel-sdk\"`\n- `new Steel({ steelAPIKey })`\n- `Steel(steel_api_key=...)`\n- explicit CDP URL construction\n- default context reuse\n- cleanup with browser close and session release\n- source-of-truth checks for exact API fields and SDK method signatures\n" },
    { path: "steel-developer/references/apis.md", text: "# Steel APIs\n\nUse this file to choose the right Steel API and avoid common implementation mistakes. Use the API reference and SDK types for exact request fields and method signatures.\n\n## Source Of Truth\n\n- API reference: https://steel.apidocumentation.com/api-reference\n- Sessions create endpoint: https://steel.apidocumentation.com/api-reference#tag/sessions/post/v1/sessions\n- Node SDK: https://github.com/steel-dev/steel-node\n- Python SDK: https://github.com/steel-dev/steel-python\n\nFetch docs with redirects:\n\n```bash\ncurl -sSfL https://docs.steel.dev/llms.txt\ncurl -sSfL https://docs.steel.dev/llms-full.txt\ncurl -sSfL https://docs.steel.dev/llms.mdx/overview/sessions-api/quickstart\n```\n\n## Routing\n\n| Need | Use |\n| --- | --- |\n| Create and release browser sessions | Sessions API |\n| Connect Playwright or Puppeteer | Sessions API plus explicit CDP URL |\n| One-shot scrape, screenshot, or PDF | Browser Tools |\n| Persist full browser state | Profiles API |\n| Reuse cookies/localStorage once | Session context |\n| Secure login injection | Credentials API |\n| CAPTCHA status and solving | CAPTCHAs API |\n| Steel-managed or BYOP proxying | Session `useProxy` |\n| Upload/download files | Files API |\n| Add Chrome extensions | Extensions API |\n| Embed live or past viewers | Session embeds |\n| Review activity timelines | Agent traces |\n\n## Gotchas\n\n- Browser Tools are stateless; use sessions for multi-step state.\n- Release sessions in cleanup.\n- Capture `sessionContext` before releasing the source session.\n- Treat session context, credentials, logs, traces, and screenshots as sensitive.\n- Extensions are organization-scoped and initialize at session start.\n- Files must be uploaded or mounted into the Steel session before browser code can access them.\n\nFor operational CAPTCHA/proxy diagnosis, use `steel-reliability`. For post-run log/trace diagnosis, use `steel-session-debugging`.\n" },
    { path: "steel-developer/references/cli-setup.md", text: "# CLI Setup\n\nSteel application code should lean on the CLI for local auth and smoke testing.\n\n```bash\nsteel --version\nsteel login\nsteel doctor --preflight\nsteel skills doctor\nsteel scrape https://example.com\n```\n\nUse `STEEL_API_KEY` in generated code. If the user is already logged in through the CLI but code needs an environment variable, explain how to export or configure a key rather than asking them to paste secrets into chat.\n\nWhen the task is project setup, verify the CLI path before writing SDK code. When the task is only code generation, still include a note that `steel doctor --preflight` should pass before running.\n" },
    { path: "steel-developer/references/computer-use.md", text: "# Computer Use\n\nKeep computer-use guidance inside `steel-developer` at launch.\n\nUse computer-use integrations when the model should reason from screenshots and emit low-level actions instead of relying on DOM selectors.\n\n## Pattern\n\n1. Create a Steel session with appropriate dimensions or mobile mode.\n2. Capture screenshots or live frame data.\n3. Send screenshots to the model.\n4. Convert model actions into Steel computer/session input APIs.\n5. Preserve session IDs and viewer URLs for debugging.\n6. Release sessions in cleanup.\n\n## Gotchas\n\n- Prefer DOM-based Playwright/Puppeteer for deterministic workflows.\n- Use computer use when the UI is visual, canvas-heavy, or selector-hostile.\n- Keep credentials and screenshots private.\n- Route failed runs to `steel-session-debugging`.\n\n## References\n\n- Claude Computer Use: https://docs.steel.dev/integrations/claude-computer-use\n- OpenAI Computer Use: https://docs.steel.dev/integrations/openai-computer-use\n- Gemini Computer Use: https://docs.steel.dev/integrations/gemini-computer-use\n" },
    { path: "steel-developer/references/ecosystem.md", text: "# Steel Ecosystem\n\nUse direct Playwright or Puppeteer as the deterministic baseline unless a higher-level framework clearly matches the user's goal.\n\n## Routing\n\n| User goal | Recommended route |\n| --- | --- |\n| Deterministic browser script | Playwright or Puppeteer |\n| Natural-language TypeScript browser actions | Stagehand |\n| Python LLM browser agent loop | Browser Use |\n| Model-native screenshot/action loop | Computer-use integrations |\n| Typed tool/agent product | Vercel AI SDK, OpenAI Agents SDK, Mastra, AgentKit, LangGraph, Pydantic AI, Agno, or CrewAI |\n| Coding agent should operate browser now | `steel-browser` |\n\nIgnore Selenium unless the user explicitly asks for it.\n\n## Baseline Rules\n\n- Create a Steel session in application code.\n- Construct `wss://connect.steel.dev?apiKey=...&sessionId=...` explicitly.\n- Connect over CDP.\n- Reuse the default context and page.\n- Release the Steel session in cleanup.\n- Add profiles, credentials, files, extensions, proxies, and CAPTCHA handling only as needed.\n\n## Links\n\n- Integrations index: https://docs.steel.dev/integrations\n- Cookbook index: https://docs.steel.dev/cookbook\n- Stagehand: https://docs.steel.dev/integrations/stagehand\n- Browser Use: https://docs.steel.dev/integrations/browser-use\n- Playwright: https://docs.steel.dev/integrations/playwright\n- Puppeteer: https://docs.steel.dev/integrations/puppeteer\n" },
    { path: "steel-developer/references/live-embed.md", text: "# Live And Past Session Embeds\n\nUse this reference when building product UI around Steel sessions.\n\n## Surfaces\n\n- live session viewer for real-time debugging or user handoff\n- past session player for replay\n- HLS recording when available\n- agent traces for timeline views\n- human-in-the-loop controls for sensitive steps\n\n## Implementation Guidance\n\n- Store session IDs and viewer/player URLs as sensitive operational data.\n- Use generated clients or API reference for exact endpoint shapes.\n- Treat replay links and screenshots as potentially sensitive.\n- Build explicit states for live, released, failed, and unavailable recordings.\n- Route post-run diagnosis to `steel-session-debugging`.\n\n## References\n\n- Embed sessions: https://docs.steel.dev/overview/sessions-api/embed-sessions\n- Live sessions: https://docs.steel.dev/overview/sessions-api/embed-sessions/live-sessions\n- Past sessions: https://docs.steel.dev/overview/sessions-api/embed-sessions/past-sessions\n- Agent traces: https://docs.steel.dev/overview/agent-traces/overview\n- Human-in-the-loop: https://docs.steel.dev/overview/sessions-api/human-in-the-loop\n" },
    { path: "steel-developer/references/python.md", text: "# Python\n\nUse this file for Python, Playwright Python, Browser Use, and Python browser-agent tasks.\n\n## Install\n\n```bash\npip install steel-sdk playwright\n```\n\nUse `STEEL_API_KEY` from the environment. Do not hardcode API keys or target-site credentials.\n\n## Playwright\n\n```python\nimport os\nfrom steel import Steel\nfrom playwright.async_api import async_playwright\n\nsteel_api_key = os.environ[\"STEEL_API_KEY\"]\nclient = Steel(steel_api_key=steel_api_key)\nsession = client.sessions.create()\ncdp_url = f\"wss://connect.steel.dev?apiKey={steel_api_key}&sessionId={session.id}\"\n\nplaywright = await async_playwright().start()\nbrowser = await playwright.chromium.connect_over_cdp(cdp_url)\ncontext = browser.contexts[0]\npage = context.pages[0] if context.pages else await context.new_page()\n\ntry:\n    await page.goto(\"https://example.com\", wait_until=\"domcontentloaded\")\n    print(await page.title())\nfinally:\n    await browser.close()\n    await playwright.stop()\n    client.sessions.release(session.id)\n```\n\nPuppeteer is not the Python path. Use Playwright for deterministic browser control or Browser Use for an agent loop.\n\n## Browser Use\n\n```python\nfrom browser_use import Agent, BrowserSession\n\nagent = Agent(\n    task=\"Find the latest news on Steel.dev\",\n    llm=llm,\n    browser_session=BrowserSession(cdp_url=cdp_url),\n)\n\nresult = await agent.run()\n```\n\nExpose CAPTCHA status helpers as Browser Use tools only when the flow needs them.\n\n## Reliability Features\n\nKeep proxy and CAPTCHA snippets minimal here. For mitigation strategy, read `../steel-reliability/references/mitigation-ladder.md` after installing `steel-reliability`.\n\n- Check plan before Steel-managed `use_proxy` or `solve_captcha`.\n- Use BYOP via `use_proxy={\"server\": os.environ[\"PROXY_SERVER\"]}` when the user supplies a proxy.\n- Poll CAPTCHA status and handle failures/timeouts explicitly.\n\n## Source Of Truth\n\n- Python SDK/types: https://github.com/steel-dev/steel-python\n- API reference: https://steel.apidocumentation.com/api-reference\n- Browser Use docs: https://docs.steel.dev/integrations/browser-use\n" },
    { path: "steel-developer/references/reliability-handoff.md", text: "# Reliability Handoff\n\nStay in `steel-developer` when the user is writing minimal implementation code and only needs the option shape for profiles, credentials, proxies, or CAPTCHA.\n\nLoad `steel-reliability` when the user is diagnosing or mitigating:\n\n- 403 or access denied responses\n- \"verify you are human\" pages\n- CAPTCHA loops or failed solves\n- Steel-managed proxy or BYOP proxy failures\n- profile reputation or persistent login problems\n- pacing, concurrency, retries, or suspicious traffic\n\nLoad `steel-session-debugging` first when the failure class is unknown or the user provides a session ID and asks what happened.\n\n## Minimal Code Here\n\nThis skill may show small snippets such as:\n\n```ts\nconst session = await client.sessions.create({\n  useProxy: { server: process.env.PROXY_SERVER! },\n});\n```\n\nBut reliability strategy, escalation, and verification belong in `steel-reliability`.\n" },
    { path: "steel-developer/references/typescript.md", text: "# TypeScript And JavaScript\n\nUse this file for Node.js, TypeScript, JavaScript, Playwright, Puppeteer, and Stagehand tasks.\n\n## Install\n\n```bash\nnpm install steel-sdk playwright puppeteer-core\n```\n\nUse `STEEL_API_KEY` from the environment. Do not hardcode API keys or target-site credentials.\n\n## Playwright\n\n```ts\nimport Steel from \"steel-sdk\";\nimport { chromium } from \"playwright\";\n\nconst steelAPIKey = process.env.STEEL_API_KEY;\nif (!steelAPIKey) throw new Error(\"STEEL_API_KEY is required\");\n\nconst client = new Steel({ steelAPIKey });\nconst session = await client.sessions.create({ timeout: 10 * 60 * 1000 });\nconst cdpUrl = `wss://connect.steel.dev?apiKey=${steelAPIKey}&sessionId=${session.id}`;\nconst browser = await chromium.connectOverCDP(cdpUrl);\nconst context = browser.contexts()[0];\nconst page = context.pages()[0] ?? (await context.newPage());\n\ntry {\n  await page.goto(\"https://example.com\", { waitUntil: \"domcontentloaded\" });\n  console.log(await page.title());\n} finally {\n  await browser.close();\n  await client.sessions.release(session.id);\n}\n```\n\n## Puppeteer\n\n```ts\nimport puppeteer from \"puppeteer-core\";\n\nconst browser = await puppeteer.connect({ browserWSEndpoint: cdpUrl });\nconst context = browser.defaultBrowserContext();\nconst page = (await context.pages())[0] ?? (await context.newPage());\n```\n\n## Credentials\n\nUse environment variables for values that may be sensitive:\n\n```ts\nawait client.credentials.create({\n  origin: \"https://app.example.com\",\n  namespace: \"example:fred\",\n  value: {\n    username: process.env.APP_USERNAME!,\n    password: process.env.APP_PASSWORD!,\n    totpSecret: process.env.APP_TOTP_SECRET,\n  },\n});\n```\n\n## Reliability Features\n\nKeep proxy and CAPTCHA snippets minimal here. For mitigation strategy, read `../steel-reliability/references/mitigation-ladder.md` after installing `steel-reliability`.\n\n- Check plan before Steel-managed `useProxy` or `solveCaptcha`.\n- Use BYOP via `useProxy: { server: process.env.PROXY_SERVER! }` when the user supplies a proxy.\n- Poll CAPTCHA status and handle failures/timeouts explicitly.\n\n## Source Of Truth\n\n- Node SDK/types: https://github.com/steel-dev/steel-node\n- API reference: https://steel.apidocumentation.com/api-reference\n- Stagehand docs: https://docs.steel.dev/integrations/stagehand\n" },
    { path: "steel-reliability/README.md", text: "# Steel Reliability Skill\n\nDiagnoses and mitigates Steel reliability issues around bot detection, CAPTCHA, proxies, profiles, credentials, login persistence, pacing, and retries.\n\n## Install\n\n```bash\nnpx skills add steel-dev/skills --skill steel-reliability\n```\n\n## Example Prompts\n\n- \"My Steel session gets a 403 on this site. Plan the least invasive mitigation.\"\n- \"Show me how to use BYOP safely on Hobby without enabling Steel-managed proxies.\"\n- \"This login keeps failing across sessions. Should I use profiles or credentials?\"\n\n## Files\n\n- `SKILL.md`: boundary, setup checks, mitigation ladder, and references.\n- `references/`: reliability runbooks and feature-specific guidance.\n- `evals/evals.json`: routing and safety assertions.\n\n## Development\n\nRun the validation script from the repository root:\n\n```bash\nnode scripts/validate-skills.mjs\n```\n" },
    { path: "steel-reliability/SKILL.md", text: "---\nname: steel-reliability\ndescription: Use this skill when a Steel automation appears unreliable because of bot detection, CAPTCHA loops, 403/access-denied or verify-you-are-human pages, proxy failures, profile or identity problems, credential or login persistence issues, suspicious pacing, redirect loops, or retry strategy, or when the user asks about stealth, proxies, CAPTCHA solving, profiles, credentials, or reliable logins. Do not use as a first step for unknown failed sessions; collect evidence with steel-session-debugging first when needed.\nlicense: MIT\ncompatibility: claude-code,codex,cursor,opencode,pi\nmetadata:\n  owner: steel\n  category: reliability\n  stage: beta\n---\n\n# Steel Reliability\n\n## Skill Boundary\n\nUse this skill to plan and verify legitimate reliability improvements for Steel browser workflows.\n\nDo not promise guaranteed bypass. Do not provide advice for evading access controls or violating site terms. Start with the least invasive change that fits the evidence.\n\nIf the user has a failed session but no evidence yet, load `steel-session-debugging` first.\n\n## Setup Check\n\n1. Confirm `steel doctor --preflight` passes.\n2. Confirm whether the user has a failed session ID, screenshot, logs, or error text.\n3. Check feature access before enabling paid CAPTCHA or Steel-managed proxy features.\n4. Preserve evidence before changing configuration.\n\n## Mitigation Ladder\n\n1. Determine whether the failure is normal auth/app behavior or likely anti-bot/reliability.\n2. Preserve evidence: status codes, redirects, challenge text, screenshots, console errors, failed requests.\n3. Use profiles when login state, cookies, or reputation matter.\n4. Add realistic pacing and lower concurrency.\n5. Add Steel-managed proxies only when feature access supports them, or BYOP when the user supplies a proxy.\n6. Add CAPTCHA solving when feature access supports it and the target flow allows it.\n7. Tune stealth config only with clear justification.\n8. Verify by rerunning the smallest failing step.\n\n## Feature Check\n\n```bash\ncurl -sSfL \"https://api.steel.dev/v1/details\" \\\n  -H \"steel-api-key: $STEEL_API_KEY\" | jq -r '.plan'\n```\n\nOn Hobby plans, do not silently enable Steel-managed `useProxy` or CAPTCHA solving. Use defaults, BYOP when supplied, profiles, pacing, or ask the user to upgrade.\n\n## References\n\n- `references/mitigation-ladder.md`: staged diagnostic and mitigation workflow.\n- `references/stealth-and-antibot.md`: challenge signals and acceptable framing.\n- `references/feature-access.md`: plan checks and feature gates.\n- `references/proxies.md`: Steel-managed proxies, geotargeting, and BYOP.\n- `references/captchas.md`: detection, status, manual solve, and failure handling.\n- `references/profiles-and-identity.md`: persistent identity and session context.\n- `references/credentials.md`: secure login injection patterns.\n- `references/pacing-and-behavior.md`: rate, wait, retry, and concurrency guidance.\n- `references/failure-signals.md`: what evidence suggests each reliability class.\n- `references/session-debugging-handoff.md`: when to collect more evidence first.\n" },
    { path: "steel-reliability/references/captchas.md", text: "# CAPTCHAs\n\nUse CAPTCHA solving only when the plan supports it and the target flow allows it.\n\n## Detect\n\nLook for:\n\n- visible reCAPTCHA, hCaptcha, or Turnstile iframes\n- \"verify you are human\" text\n- repeated challenge redirects\n- session CAPTCHA status showing detected or solving tasks\n\n## Auto Solve\n\n```ts\nconst session = await client.sessions.create({\n  solveCaptcha: true,\n});\n```\n\n## Manual Mode\n\nUse manual mode when the workflow needs to inspect status and trigger a specific task:\n\n```ts\nconst session = await client.sessions.create({\n  solveCaptcha: true,\n  stealthConfig: { autoCaptchaSolving: false },\n});\n\nconst states = await client.sessions.captchas.status(session.id);\nawait client.sessions.captchas.solve(session.id, { taskId });\n```\n\n## Failure Handling\n\n- Check plan first.\n- Handle failed statuses and timeouts explicitly.\n- Combine CAPTCHA solving with profiles and realistic pacing when login reputation matters.\n- Do not retry the same CAPTCHA loop indefinitely.\n" },
    { path: "steel-reliability/references/credentials.md", text: "# Credentials\n\nUse Steel Credentials when login secrets should not be exposed to the agent, generated code, page scripts, or logs.\n\n## Pattern\n\n1. Store credentials once per origin and namespace.\n2. Create the session with the same namespace and `credentials` enabled.\n3. Navigate to the login page.\n4. Wait for injection.\n5. Assert login success.\n\n## Rules\n\n- Use environment variables for usernames, passwords, and TOTP secrets in generated setup code.\n- Do not paste raw credentials into chat.\n- Do not fill username/password manually when using Steel Credentials.\n- Keep namespace stable for the account and target origin.\n\n## Reliability Notes\n\nCredentials solve secret handling; profiles solve persistent browser identity. Many reliable login flows use both.\n" },
    { path: "steel-reliability/references/failure-signals.md", text: "# Failure Signals\n\n## Likely Auth Or State\n\n- 401 after login\n- redirect back to login\n- missing expected cookies\n- profile ID absent or wrong namespace\n- dashboard loads but user data is missing\n\nStart with profiles, credentials, and session context.\n\n## Likely Bot Detection\n\n- 403 or access denied\n- verify-human page\n- challenge URLs or iframes\n- empty results only on automated runs\n- repeated redirects through challenge pages\n\nCollect evidence, then use the mitigation ladder.\n\n## Likely Proxy\n\n- `ERR_TUNNEL_CONNECTION_FAILED`\n- proxy auth error\n- target works without proxy but fails with proxy\n- geolocation mismatch\n\nCheck BYOP credentials, Steel-managed feature access, and geotargeting.\n\n## Likely CAPTCHA\n\n- CAPTCHA iframe visible\n- solve status failed or timed out\n- repeated CAPTCHA after solve\n\nCheck plan, solve mode, pacing, and profile reputation.\n" },
    { path: "steel-reliability/references/feature-access.md", text: "# Feature Access\n\nCheck plan access before enabling Steel-managed proxies or CAPTCHA solving.\n\n```bash\ncurl -sSfL \"https://api.steel.dev/v1/details\" \\\n  -H \"steel-api-key: $STEEL_API_KEY\" | jq -r '.plan'\n```\n\n## Rules\n\n- `STEEL_API_KEY` or `steel login` must be configured before checking.\n- Hobby plans should not silently enable Steel-managed proxies or CAPTCHA solves.\n- BYOP is distinct from Steel-managed proxy bandwidth and can be used when the user supplies a proxy server.\n- When access is missing, explain the fallback instead of generating code that will fail.\n\n## Fallbacks\n\n- default datacenter networking\n- profiles and credentials\n- lower concurrency and better pacing\n- BYOP via an environment variable\n- user upgrade or plan change\n" },
    { path: "steel-reliability/references/mitigation-ladder.md", text: "# Mitigation Ladder\n\nUse the smallest change that fits the evidence.\n\n## 1. Confirm The Failure Class\n\n- Normal auth issue: 401, expired login, missing cookies, wrong tenant.\n- App issue: validation error, disabled button, broken selector, modal.\n- Reliability issue: 403, access denied, challenge page, CAPTCHA loop, proxy tunnel error, suspicious traffic warning.\n\nIf evidence is missing, load `steel-session-debugging`.\n\n## 2. Preserve Evidence\n\nCapture status codes, redirects, challenge text, screenshots, console errors, failed requests, and the final URL before changing configuration.\n\n## 3. Identity First\n\nUse profiles for repeated login state and cookies. Use credentials for secure form injection. Avoid adding proxies before fixing missing auth state.\n\n## 4. Behavior Next\n\nLower concurrency, add realistic waits, reduce repeated identical retries, and verify each step before escalating.\n\n## 5. Network And CAPTCHA\n\nAdd Steel-managed proxies only when the plan supports them. Use BYOP when the user supplies a proxy. Add CAPTCHA solving only when the plan and target flow support it.\n\n## 6. Verify\n\nRerun the smallest failing step. Report what changed, what evidence improved, and what remains uncertain.\n" },
    { path: "steel-reliability/references/pacing-and-behavior.md", text: "# Pacing And Behavior\n\nMany failures come from acting faster or more repetitively than the target flow expects.\n\n## Improvements\n\n- Wait for visible page state, not only fixed time.\n- Add short human-scale delays between repeated form actions.\n- Lower concurrency per domain.\n- Avoid identical rapid retries after blocked responses.\n- Reuse authenticated profiles instead of logging in repeatedly.\n- Verify after each important transition.\n\n## Retry Policy\n\nRetry when the evidence indicates a transient failure:\n\n- proxy tunnel error\n- 5xx response\n- network reset\n- navigation timeout on an otherwise healthy route\n\nDo not retry indefinitely after:\n\n- account lockout\n- repeated CAPTCHA loop\n- clear access denied page\n- invalid credentials\n\nEscalate one factor at a time so the next run teaches you something.\n" },
    { path: "steel-reliability/references/profiles-and-identity.md", text: "# Profiles And Identity\n\nUse profiles when reliability depends on persistent browser identity: cookies, login state, extension state, settings, and reputation.\n\n## Pattern\n\n1. Create a session with `persistProfile: true`.\n2. Complete login or setup.\n3. Release the session so Steel persists the profile.\n4. Reuse future sessions with `profileId`.\n5. Include `persistProfile: true` again when the session should update stored state.\n\n## Session Context\n\nUse `sessionContext` for lighter cookie/localStorage transfer between sessions. Capture it before releasing the source session. Treat it as sensitive data.\n\n## Reliability Guidance\n\n- Fix missing auth/profile state before adding proxies.\n- Use one profile per user/account namespace.\n- Avoid sharing profiles across unrelated target sites.\n- Verify login success with a page assertion after every reused profile session.\n" },
    { path: "steel-reliability/references/proxies.md", text: "# Proxies\n\nStart without proxies unless the evidence points to IP reputation, geolocation, or network blocking.\n\n## Steel-Managed Proxy\n\nUse after plan access is confirmed:\n\n```ts\nconst session = await client.sessions.create({ useProxy: true });\n```\n\nPrefer broad geotargeting before narrow geotargeting:\n\n```ts\nconst session = await client.sessions.create({\n  useProxy: { geolocation: { country: \"US\" } },\n});\n```\n\n## Bring Your Own Proxy\n\nUse BYOP when the user supplies a proxy or the plan does not include Steel-managed proxy bandwidth:\n\n```ts\nconst session = await client.sessions.create({\n  useProxy: { server: process.env.PROXY_SERVER! },\n});\n```\n\nKeep proxy credentials in environment variables. Do not print proxy URLs with credentials in logs.\n\n## Failure Handling\n\n- Retry transient `ERR_TUNNEL_CONNECTION_FAILED`.\n- Fall back to default networking if proxy failures dominate.\n- Preserve status code and target host before swapping.\n- Do not rotate repeatedly without changing any other factor.\n" },
    { path: "steel-reliability/references/session-debugging-handoff.md", text: "# Session Debugging Handoff\n\nLoad `steel-session-debugging` before `steel-reliability` when:\n\n- the user only says \"it failed\" without evidence\n- there is a session ID but no known failure class\n- you need logs, traces, replay, or screenshot evidence\n- the problem may be a selector, wait, app, or session lifecycle bug\n\n## Handoff Prompt\n\n```text\nUse steel-session-debugging to collect metadata, browser logs, agent logs, traces, and replay evidence for session <id>. Redact sensitive values, classify the failure, then return here only if the evidence points to bot detection, proxy, CAPTCHA, identity, pacing, or login reliability.\n```\n\nDo not skip evidence collection just because the target site is known to be bot-sensitive.\n" },
    { path: "steel-reliability/references/stealth-and-antibot.md", text: "# Stealth And Anti-Bot\n\nFrame this as reliability for legitimate automation. Do not promise bypass.\n\n## Signals\n\n- 403 or access denied after navigation\n- \"verify you are human\" page\n- Cloudflare, PerimeterX, DataDome, Akamai, or similar challenge copy\n- redirect loops to challenge pages\n- successful login followed by immediate logout or blocked dashboard\n- empty content while browser logs show blocked API calls\n\n## Safe Strategy\n\n1. Confirm the signal with logs, screenshot, or trace.\n2. Check feature access.\n3. Use profiles for persistent identity.\n4. Slow down repeated actions and reduce concurrency.\n5. Add proxy/CAPTCHA features only when supported and justified.\n6. Verify with a focused rerun.\n\n## Avoid\n\n- claims that Steel can guarantee access to any site\n- advice to evade paywalls, account bans, or access controls\n- adding all stealth options before collecting evidence\n- treating every timeout as bot detection\n" },
    { path: "steel-session-debugging/README.md", text: "# Steel Session Debugging Skill\n\nDiagnoses failed Steel browser sessions from metadata, logs, traces, replay links, and network evidence.\n\n## Install\n\n```bash\nnpx skills add steel-dev/skills --skill steel-session-debugging\n```\n\n## Example Prompts\n\n- \"Debug Steel session `sess_123` and tell me why login failed.\"\n- \"This run timed out after clicking submit. Use the session ID and find the cause.\"\n- \"Summarize the agent traces and browser logs for this failed run.\"\n\n## Files\n\n- `SKILL.md`: routing, setup checks, workflow, and output format.\n- `references/`: investigation, logs, traces, network, replay, taxonomy, and reliability handoff.\n- `scripts/`: local collection, redaction, and summary helpers.\n- `evals/evals.json`: routing and behavior assertions.\n\n## Development\n\nRun the validation script from the repository root:\n\n```bash\nnode scripts/validate-skills.mjs\n```\n" },
    { path: "steel-session-debugging/SKILL.md", text: "---\nname: steel-session-debugging\ndescription: Use this skill when the user provides a failed Steel session ID, failed automation run, timeout, browser error, blocked page, or unexpected result and wants evidence-backed diagnosis from session metadata, browser logs, raw agent logs, semantic traces, replay links, screenshots, network failures, or errors. Do not use for first-pass bot mitigation; collect evidence here, then hand off to steel-reliability for proxy, CAPTCHA, identity, or anti-bot fixes.\nlicense: MIT\ncompatibility: claude-code,codex,cursor,opencode,pi\nmetadata:\n  owner: steel\n  category: debug\n  stage: beta\n---\n\n# Steel Session Debugging\n\n## Skill Boundary\n\nUse this skill to explain what happened in a failed Steel session and what to change next. Start from evidence, not guesses.\n\nDo not use this skill as the primary place for bot-detection mitigation, proxy strategy, CAPTCHA strategy, login reputation, or retry policy. If the evidence points there, collect enough proof and hand off to `steel-reliability`.\n\n## Setup Check\n\n1. Confirm the user provided a Steel session ID or enough context to identify one.\n2. Run `steel doctor --preflight` if CLI auth or API access is uncertain.\n3. Prefer Steel CLI diagnostics before raw API calls.\n4. Create local artifacts under `.steel-debug/`; do not paste raw logs into chat unless the user asks.\n\n## Investigation Workflow\n\n1. Collect session metadata, browser logs, raw agent logs, semantic agent traces, and viewer/replay links.\n2. Redact cookies, bearer tokens, API keys, passwords, form values, and obvious PII before summarizing.\n3. Build a timeline: session creation, navigations, actions, waits, console errors, failed requests, CAPTCHA/proxy signals, and final state.\n4. Classify the failure using the taxonomy in `references/failure-taxonomy.md`.\n5. Produce an evidence-backed diagnosis with a smallest useful verification step.\n6. If the fix involves bot detection, proxy, CAPTCHA, identity, pacing, or login persistence, route to `steel-reliability`.\n\n## Output Format\n\n```text\nDiagnosis:\n- What happened:\n- Most likely cause:\n- Evidence:\n- What to change:\n- Verification step:\n- Confidence:\n```\n\n## Useful Commands\n\n```bash\nsteel --json sessions get <session-id>\nsteel --json sessions logs <session-id>\nsteel --json sessions agent-logs <session-id>\nsteel --json sessions traces <session-id>\nnode scripts/collect-session-debug.mjs <session-id>\nnode scripts/redact-session-debug.mjs .steel-debug/<session-id>/bundle.json\nnode scripts/summarize-session-debug.mjs .steel-debug/<session-id>/redacted-bundle.json\n```\n\n## References\n\n- `references/investigation-workflow.md`: detailed collection and timeline process.\n- `references/log-event-types.md`: how to separate metadata, browser logs, raw agent logs, and traces.\n- `references/agent-traces.md`: how to read semantic agent trace events.\n- `references/network-forensics.md`: request failures, redirects, and blocked-page evidence.\n- `references/replay-and-screenshots.md`: viewer, player, HLS, and screenshot usage.\n- `references/failure-taxonomy.md`: standard failure classes.\n- `references/reliability-handoff.md`: when to load `steel-reliability`.\n" },
    { path: "steel-session-debugging/references/agent-traces.md", text: "# Agent Traces\n\nAgent traces are the first place to understand what the automation attempted.\n\n## What To Inspect\n\n- action type: click, fill, type, wait, navigate, extract, screenshot\n- target element accessible name\n- selector candidates and element refs\n- input values after redaction\n- URL before and after the action\n- idle gaps and wait points\n- action result, error, or timeout\n\n## Common Trace Findings\n\n- stale selector or `@eN` ref used after navigation\n- click landed on the wrong matching element\n- wait condition too weak or too strict\n- form submit caused a redirect to a challenge/login page\n- page changed shape between snapshot and action\n- action succeeded but extraction read from the old page state\n\n## Diagnosis Pattern\n\nQuote the action and result in plain English:\n\n```text\nThe agent clicked \"Continue\" at 12:04:18, then waited for network idle. The next trace event shows the URL changed to /challenge instead of /dashboard.\n```\n\nThen connect it to the browser/network evidence before recommending a fix.\n" },
    { path: "steel-session-debugging/references/failure-taxonomy.md", text: "# Failure Taxonomy\n\nUse one primary class and optional secondary classes.\n\n## Setup And Platform\n\n- setup/auth failure\n- API unreachable or invalid key\n- invalid or expired session\n- session ended early\n- unavailable logs or traces\n- platform-side incident\n\n## Browser And Page\n\n- page never loaded\n- navigation timeout\n- wrong page state\n- JavaScript/runtime error\n- network/API failure\n- file upload/download failure\n- popup, modal, or tab focus problem\n\n## Agent Behavior\n\n- stale element ref or selector\n- wrong target clicked or typed into\n- wait condition mismatch\n- extraction read from old state\n- incomplete cleanup or unreleased session\n\n## Auth And Reliability\n\n- login/auth state missing\n- profile/session context mismatch\n- CAPTCHA not solved\n- bot detection or access denied\n- proxy failure\n- suspicious pacing or concurrency\n\nRoute auth/reliability classes to `steel-reliability` after collecting enough evidence.\n" },
    { path: "steel-session-debugging/references/investigation-workflow.md", text: "# Investigation Workflow\n\n## 1. Confirm Scope\n\n- Confirm the session ID.\n- Ask for the expected outcome if the user did not provide it.\n- Run `steel doctor --preflight` when auth or API access is uncertain.\n\n## 2. Collect Evidence\n\nPrefer CLI commands:\n\n```bash\nsteel --json sessions get <session-id>\nsteel --json sessions logs <session-id>\nsteel --json sessions agent-logs <session-id>\nsteel --json sessions traces <session-id>\n```\n\nUse `scripts/collect-session-debug.mjs` when you need a local bundle:\n\n```bash\nnode scripts/collect-session-debug.mjs <session-id>\n```\n\n## 3. Redact Before Sharing\n\nRun:\n\n```bash\nnode scripts/redact-session-debug.mjs .steel-debug/<session-id>/bundle.json\n```\n\nDo not paste raw logs, cookies, auth headers, passwords, TOTP values, session context, or form values into chat unless the user explicitly requests it.\n\n## 4. Build A Timeline\n\nInclude:\n\n- session status, start time, release/failure time\n- navigations and redirects\n- agent actions and waits\n- console/page errors\n- failed network requests\n- CAPTCHA/proxy/auth signals\n- final page state or last known URL\n\n## 5. Diagnose\n\nClassify the failure, cite evidence, and recommend the smallest next run that can verify the fix.\n\nAvoid unsupported guesses. If a surface is unavailable, say that and lower confidence.\n" },
    { path: "steel-session-debugging/references/log-event-types.md", text: "# Log Event Types\n\n## Session Metadata\n\nUse `steel --json sessions get <session-id>` for lifecycle status, creation time, URLs, region, profile, proxy status, and release/failure state.\n\n## Browser Logs\n\nUse `steel --json sessions logs <session-id>` for page and browser events. Look for:\n\n- console errors\n- navigation errors\n- failed requests\n- page crashes\n- timeout markers\n- CAPTCHA/proxy events\n\n## Raw Agent Logs\n\nUse `steel --json sessions agent-logs <session-id>` for CDP-derived event streams. Raw logs are useful when the semantic trace is incomplete or when you need exact low-level timing.\n\n## Agent Traces\n\nUse `steel --json sessions traces <session-id>` for semantic actions such as clicks, fills, navigations, waits, extracted text, accessible names, selectors, and result summaries.\n\n## How To Separate Them\n\n- Metadata answers \"what session was this?\"\n- Browser logs answer \"what did the page/browser report?\"\n- Raw agent logs answer \"what low-level events occurred?\"\n- Agent traces answer \"what did the agent intend and do?\"\n\nNever blend them into one vague \"logs say\" claim. Name the surface that provides the evidence.\n" },
    { path: "steel-session-debugging/references/network-forensics.md", text: "# Network Forensics\n\nUse network evidence to distinguish normal app errors from Steel/session/reliability failures.\n\n## Signals To Collect\n\n- final URL and redirect chain\n- HTTP 401, 403, 407, 429, and 5xx responses\n- failed requests and browser error codes\n- challenge URLs or parameters such as `cf-chl`, `chal_t`, `/sorry/`, or `/captcha`\n- proxy errors such as `ERR_TUNNEL_CONNECTION_FAILED`\n- blank content with successful status, which may indicate client-side rendering or blocked JS\n\n## Interpretation\n\n- `401` after login usually means missing/expired auth state, not necessarily bot detection.\n- `403`, challenge redirects, or access-denied copy often need `steel-reliability`.\n- `407` or tunnel failures point toward proxy configuration.\n- Repeated failed asset/API calls can explain empty pages even when the main document loads.\n\n## Output\n\nInclude at most the relevant URLs, status codes, and error strings. Redact query parameters when they include tokens, emails, account IDs, or session context.\n" },
    { path: "steel-session-debugging/references/reliability-handoff.md", text: "# Reliability Handoff\n\nStay in `steel-session-debugging` when the task is to collect evidence and explain what happened.\n\nLoad `steel-reliability` when the next step is to design or implement mitigation for:\n\n- bot detection\n- access denied or challenge pages\n- CAPTCHA loops or failed solves\n- Steel-managed proxy or BYOP proxy failures\n- persistent login/profile identity problems\n- suspicious pacing, high concurrency, or retry policy\n- reputation-sensitive domains\n\n## Handoff Format\n\n```text\nEvidence collected:\n- ...\n\nWhy this is a reliability problem:\n- ...\n\nLoad steel-reliability to plan the mitigation ladder.\n```\n\nDo not promise guaranteed bypass. Frame the next step as legitimate automation reliability and verification.\n" },
    { path: "steel-session-debugging/references/replay-and-screenshots.md", text: "# Replay And Screenshots\n\nReplay artifacts help verify the timeline visually.\n\n## Useful Surfaces\n\n- session viewer URL for live or recent state\n- player URL for past replay\n- HLS recording when available\n- screenshots taken by the agent or browser command\n- final page URL and visible text from the last snapshot\n\n## What To Look For\n\n- a modal or cookie banner covering the intended target\n- a challenge page after a successful-looking click\n- wrong tab or popup focus\n- form validation messages\n- missing uploaded file or disabled submit button\n- UI state that diverges from the trace's intended action\n\n## Sharing Guidance\n\nDo not export screenshots or recordings outside the local machine unless the user asks. Screenshots may contain personal data, login state, or page content under contract.\n" },
    { path: "steel-skill-creator/README.md", text: "# Steel Skill Creator\n\nCompiles a recurring web task into a reusable, parameterized agent skill by driving the task twice in Steel, comparing traces, authoring a concise `SKILL.md`, and verifying with a third run.\n\n## Install\n\n```bash\nnpx skills add steel-dev/skills --skill steel-skill-creator\n```\n\n## Example Prompts\n\n- \"Turn my weekly vendor price check into a reusable skill.\"\n- \"Make a skill from this recurring login-gated report workflow.\"\n- \"Capture this browser task twice and generate a parameterized skill.\"\n\n## Files\n\n- `SKILL.md`: two-trace creation workflow.\n- `references/`: trace anatomy, Steel primitives, authoring, parameterization, evals, and verification.\n- `scripts/`: trace fetch, trace compare, scaffold, validate, and install helpers.\n- `templates/browser-task-skill/`: starter scaffold for generated browser task skills.\n- `evals/evals.json`: creation workflow assertions.\n\n## Development\n\nRun validation from the repository root:\n\n```bash\nnode scripts/validate-skills.mjs\n```\n" },
    { path: "steel-skill-creator/SKILL.md", text: "---\nname: steel-skill-creator\ndescription: Use this skill when the user wants to turn a recurring browser workflow into a reusable, parameterized agent skill, especially when the task has concrete inputs and a clear output such as scheduled scrapes, form submissions, data extraction, monitoring flows, price probes, or login-gated reports. Do not use for one-off web tasks; use steel-browser.\nlicense: MIT\ncompatibility: claude-code,codex,cursor,opencode,pi\nmetadata:\n  owner: steel\n  category: create\n  stage: beta\n---\n\n# Steel Skill Creator\n\nTurn a recurring web task into an agent skill. The user describes the task and one set of example inputs. You drive the task end-to-end twice in a real Steel browser (once with the example inputs, once with mutated inputs), capture both traces through the Steel CLI, author a parameterized SKILL.md, install it, and verify it on a third input set.\n\nThe user does not provide a session ID. You generate both sessions yourself.\n\n## The principle this skill is built on\n\nSteel agent traces are already 80% of a SKILL.md. They contain stable selectors prioritized by quality (testId → id → aria → name → CSS), accessibleNames for every clicked element, page boundaries, idle gaps that mark wait points, and parameter values inline as URL query strings and form inputs. Your job is mostly **reading two traces side by side and writing the parts that aren't there yet**: the goal, the parameter names, the success criteria, and the right Steel session configuration (stealth, proxy, credentials, profiles) for production replay.\n\nThe reason you generate *both* traces yourself: parameter extraction is reliable when you can diff two runs of the same flow with different inputs. Anything that differs at corresponding positions is a parameter; anything identical is an invariant. You can't get that from a single trace without guessing, and asking the user to record twice doubles their effort. Better that you do it.\n\nDo not try to handle every edge case programmatically. Use your judgment. The references/ directory tells you what to look for; the rest is reading code and writing code.\n\n## Prerequisites\n\n- `steel` CLI is installed and authenticated for browser driving.\n- The `steel-browser` skill is available — you'll use the same primitives (`steel browser start`, `navigate`, `snapshot`, `click`, `fill`, `wait`, etc.) to drive both recording sessions.\n- The task is described clearly enough that you can execute it. If the description is too vague (\"automate my browsing\"), ask for specifics before doing anything else.\n\n## Workflow\n\n### Step 1 — Gather task description and example inputs\n\nYou need three things from the user:\n\n1. **Skill name** — a kebab-case identifier (e.g., `flight-price-probe`, `weekly-traffic-report`). If the user has not given one, propose one based on what the flow does and confirm.\n2. **One-line goal** — what the skill is for, in the user's own words (\"find cheapest direct flight between two airports on a given date range\"). This becomes the basis of the generated skill's description.\n3. **One set of example inputs** — concrete values that exercise the full flow. Not \"search for flights\"; \"search SPU → FCO, depart 2026-06-13, return 2026-06-18\". Be specific enough that you can actually execute the task without asking again.\n\nIf anything is missing or ambiguous, ask once, concisely. Don't drag the user through a long Q&A.\n\n### Step 2 — Confirm safety before recording\n\nBefore running the task even once, think about side effects:\n\n- **Read-only flow** (scraping, searching, reading reports, price lookups) → safe to run twice with different inputs. Proceed.\n- **Side-effect flow** (booking, paying, sending messages, submitting forms that trigger emails or charges, creating accounts, posting content) → **stop and ask.** Running this twice with arbitrary inputs could cost money, spam someone, or create real-world artifacts. Offer the user three options:\n  1. Use sandbox/test accounts/inputs for both runs.\n  2. Record once only, accept lower-quality parameterization (the generated skill will have to guess some parameters from heuristics rather than diff evidence), and clearly mark the skill as \"single-trace draft\" in its description.\n  3. Abandon the automation here and describe the steps in plain English as documentation rather than an executable skill.\n\nNever silently run a side-effect flow twice. The user's trust is more important than the skill.\n\n### Step 3 — Record trace #1 by driving the task yourself\n\nUse the steel-browser primitives to perform the task end-to-end with the example inputs from step 1. Start a session (with the right configuration per `references/steel-primitives.md`), navigate, click, wait, extract — exactly the same patterns you'd use if you were running `/steel-browser` for the user.\n\nTake screenshots and snapshots along the way to verify each step worked. If a step fails, debug it like you would in any normal Steel session — don't push through a broken state.\n\nWhen the task reaches its success state (the data is extracted, the form is submitted, the confirmation page renders), stop the session. **Save the session ID** — you'll need it in step 5.\n\n### Step 4 — Record trace #2 by replaying with mutated inputs\n\nPick mutated inputs. For each example value from step 1, choose a sensible alternative that exercises the same flow:\n\n- Date → different date in the same or adjacent month.\n- Search term → semantically different but well-formed (different airport, different product category, different person's name).\n- IDs → a different known-good ID.\n\nAim for *different inputs, same intent*. The goal is a trace that takes the same conceptual path so the diff isolates parameters cleanly.\n\nRun the task again in a fresh Steel session using the mutated inputs. Same configuration, same starting URL, same logical sequence of actions.\n\n**If trace #2 diverges from trace #1** — hits a login wall trace #1 didn't, encounters a CAPTCHA, lands on a structurally different page — stop and investigate. Consult `references/steel-primitives.md` to decide whether to add Steel session options (stealth, proxy, captcha solving) or whether to escalate to the user. Do not paper over the divergence.\n\nSave the second session ID.\n\n### Step 5 — Fetch both traces\n\n```bash\nnode scripts/fetch_trace.mjs <session-id-1>\nnode scripts/fetch_trace.mjs <session-id-2>\n```\n\nThe script calls `steel --json sessions traces <session-id>`, writes the normalized trace JSON to a temp file, and prints the path. The trace has events with `accessibleName`, selectors, page URLs, value fields, and timestamps — see `references/trace-anatomy.md` for what to look at.\n\n### Step 6 — Author the generated skill\n\nYou now have two traces. Read them together. The differences at corresponding positions are the **parameters**; the similarities are the **invariants**.\n\nConsult these references in order:\n- `references/trace-anatomy.md` — how to read each event and how to align two traces.\n- `references/steel-primitives.md` — the decision tree for credentials, profiles, stealth, proxy, and CAPTCHA. This determines how the generated skill creates its Steel session.\n- `references/skill-template.md` — the scaffold to fill in.\n\nThe generated skill depends on the `steel-browser` skill at runtime — it does not embed CLI commands inline. Every generated skill must include a `## Prerequisites` section (the template shows the exact wording) that points the user at `curl -fsS https://setup.steel.dev | sh`, which installs the steel CLI and the steel-browser skill. The body steps then describe intent in plain prose, with element names in italics; the executor translates that into `snapshot → click → fill → wait` sequences using steel-browser's primitives.\n\nSelector choice for the generated skill, in priority order:\n1. Element text or accessibleName (most resilient — survives most DOM rewrites).\n2. `data-testid` if present.\n3. `id` if present and looks intentional.\n4. `aria-label` or `name`.\n5. CSS selectors as a last resort, never `:nth-of-type` chains unless nothing else exists.\n\nUse `target.selector` as evidence for available selectors, but prefer a readable semantic step when the accessible name is specific enough. For example, write \"Click the *Direct only* checkbox\" instead of exposing a raw selector.\n\nName parameters by what they *mean*, not what they *are*. `depart_date`, not `param_2`. The model that will later use the generated skill is reading these names cold; clarity matters.\n\nWrite the goal in the imperative (\"Find the cheapest direct round-trip price for {origin} → {destination} between {depart} and {return}\"). Write success criteria in terms of *what gets returned*, not what gets clicked.\n\n### Step 7 — Install the skill\n\n```bash\nnode scripts/install_skill.mjs <skill-name> --skill-md <path-to-generated-SKILL.md>\n```\n\nThe script writes to `~/.claude/skills/<skill-name>/` and tells you the final path. Generated skills go system-wide by default so the user can invoke them from any project.\n\n### Step 8 — Verify by running the new skill\n\nPick a *third* set of inputs — distinct from both step 1 (example inputs) and step 4 (mutated inputs). Invoke the freshly-installed skill against those inputs in a fresh Steel session. Capture the resulting session ID, fetch trace #3.\n\n### Step 9 — Judge fidelity\n\nRead `references/llm-judge-rubric.md`. With the three traces, the generated skill source, and the inputs/outputs you've gathered, form a verdict:\n- Did trace #3 reach the same kind of success signal as traces #1 and #2?\n- Did the third inputs substitute cleanly into URLs and form values?\n- Were extra steps (cookie banners, modals) handled or did they break things?\n- Were any steps skipped, and if so, did that matter?\n\nDo not run a structural diff. Form a judgment.\n\nIf the verdict is good: tell the user the skill is installed, where, and show one example invocation with concrete inputs.\n\nIf the verdict is mixed: show the user the diagnosis in plain English and offer to revise — usually the fix is one of: a wait point in the wrong place, a selector that was too specific, a parameter that should have been split into two, or a Steel session option that was missed.\n\n## Decision points: when to ask the user vs. proceed\n\nAsk the user when:\n\n- The task description is vague enough that you'd have to invent the inputs (step 1).\n- The task has side effects you can't safely replay twice (step 2).\n- The first run hits a login flow on a site Steel credentials are not configured for. You need consent before storing credentials in the vault.\n- The replay in step 4 hits a CAPTCHA on a site the user is not on a paid Steel plan for, or a geo-block requiring a region they haven't authorized.\n- You cannot tell from the two traces whether something was a deliberate parameter or accidental noise (e.g., did the user scroll to position 437px on purpose, or was that incidental?).\n\nProceed without asking when:\n\n- Selector choice — use your judgment from the priority list.\n- Parameter naming — propose names, the user will rename them later if they want.\n- Wait point placement — idle gaps + page transitions are reliable signals.\n- Mutated input choice in step 4 — pick something reasonable and move on.\n- Whether to enable stealth/proxy/CAPTCHA on the generated skill — the decision tree in `references/steel-primitives.md` is unambiguous.\n\n## What to avoid\n\n- **Do not regex over trace JSON looking for parameters.** Read the traces; reason about them. Two-trace diff is a mental operation, not a string-comparison operation.\n- **Do not include trace IDs, session IDs, or timestamps in the generated skill.** Those are artifacts of *this* compilation; they don't belong in the reusable skill.\n- **Do not write a hundred-line SKILL.md.** Generated skills should be lean — 40–80 lines of body. The Steel trace is the source code; the SKILL.md is the abstract.\n- **Do not invent steps that weren't in either trace.** If neither recording included \"dismiss the cookie banner\", the generated skill shouldn't pretend it did. If a banner shows up later in production, the verification pass will catch it.\n- **Do not run a side-effect flow twice without user consent.** This bears repeating.\n\n## Output\n\nA successful run leaves the user with:\n\n- A new skill at `~/.claude/skills/<skill-name>/`.\n- An example invocation they can copy-paste.\n- A short note on any caveats (e.g., \"requires Steel credentials for foo.com to be configured; run `steel credentials create --origin https://foo.com` once before first use\").\n- A one-line fidelity verdict from the verification pass.\n" },
    { path: "steel-skill-creator/references/authoring-workflow.md", text: "# Authoring Workflow\n\n1. Gather a clear recurring task, success criteria, allowed domains, and one example input set.\n2. Decide whether the flow is safe to record twice. Ask before side-effecting flows.\n3. Record trace one with the example inputs.\n4. Record trace two with changed inputs and the same intent.\n5. Compare traces for stable steps, variables, selectors, auth assumptions, and wait points.\n6. Scaffold a concise generated skill.\n7. Write evals for happy path, changed parameter, and common failure.\n8. Install locally and verify with a third input set.\n\nGenerated `SKILL.md` files should be short, imperative, and one level deep. Put detailed examples or scripts in references.\n" },
    { path: "steel-skill-creator/references/eval-writing.md", text: "# Eval Writing\n\nEvery generated skill should have at least three evals:\n\n1. Happy path with the example input shape.\n2. Changed parameter path with different inputs.\n3. Common failure or negative routing path.\n\n## Assertions\n\nUse the `assertions` key. Assertions should check behavior, not exact prose.\n\nGood assertions:\n\n- \"Requests all required inputs before starting\"\n- \"Uses steel-browser primitives for live execution\"\n- \"Does not hardcode the example date\"\n- \"Verifies the success signal before finishing\"\n\nAvoid:\n\n- exact paragraphs\n- screenshots as the only success criterion\n- assertions that require hidden credentials\n- claims that cannot be observed from the agent transcript\n" },
    { path: "steel-skill-creator/references/llm-judge-rubric.md", text: "# Fidelity rubric for the verification pass\n\nUse this when judging whether a generated skill works. You have:\n- Trace #1 (your first run, with the user's example inputs)\n- Trace #2 (your second run, with mutated inputs)\n- Trace #3 (the verification run by invoking the freshly-installed skill on a *third* input set)\n- The generated skill source\n- The inputs passed to trace #3 and the output the skill returned\n\nDo not run a structural diff. Form a judgment. The goal is \"did the skill achieve the user's intent on new inputs\", not \"did every event match position-by-position\".\n\n## Criteria, in order of importance\n\n### 1. Success signal reached\n\nThe most important question: did trace #3 end in the same kind of state as traces #1 and #2?\n\n- A price was extracted and looks plausible.\n- A confirmation page was reached.\n- A file was downloaded.\n- A form submission was acknowledged.\n- The expected element appeared on the final page.\n\nIf yes: this dominates. The skill works on the new inputs.\nIf no: nothing below matters until this is fixed. Go figure out why.\n\n### 2. Parameter substitution clean\n\nDid the third inputs show up in the right places? Easiest checks:\n- Search for the new input values as strings in trace #3's URLs and DOM. They should appear where the corresponding old values appeared in trace #1.\n- If a parameter was supposed to template into a URL and didn't, the skill has a parameterization bug.\n- If a parameter shows up in trace #3 but in the *wrong* position (e.g., destination swapped with origin), the skill mapped the parameters incorrectly.\n\n### 3. Wait points held up\n\nDid the generated skill wait for the right things? Signs of failure:\n- Steps that tried to interact with an element before it existed (you'd see this as a timeout or a missing-element error).\n- Long idle gaps in trace #3 that weren't present in trace #1 (the skill waited for the wrong thing or used the wrong timeout).\n- Race conditions: a click that succeeded but didn't have the expected effect because the page wasn't ready.\n\n### 4. Extra steps handled gracefully\n\nA new modal, cookie banner, or \"rate this experience\" popup that wasn't in trace #1 may have appeared in trace #3. Did the skill handle it, fail because of it, or finish anyway by accident?\n\n- Handled it: good. Note that the skill picked up a real-world adaptation.\n- Failed because of it: the skill needs to learn to dismiss unknown overlays. Suggest adding a generic \"dismiss non-essential modals before key interactions\" step.\n- Finished anyway: fine, but flag it — next time it might not.\n\n### 5. Skipped steps explained\n\nIf trace #3 is materially shorter than traces #1 and #2, ask:\n- Did the skill find a shortcut that achieves the goal in fewer steps? Fine.\n- Did the skill skip a step that was actually needed, and only got away with it because of cached state? Risky — flag for the user.\n- Did the skill bail out early after a failed step? Failure. Go back to criterion 1.\n\n### 6. Unexpected state handled via the right Steel primitive\n\nIf a CAPTCHA appeared in trace #3 that wasn't in trace #1, did the skill solve it (via `solve_captcha: true`) or crash?\nIf a login wall appeared, did the skill use the credentials vault or stop with a helpful error?\n\nThe right answer here is \"the skill picked the right Steel primitive\". The wrong answer is \"the skill tried to type the password directly into the form\" or \"the skill ignored the CAPTCHA\".\n\n## Output format for your verdict\n\nTell the user a short verdict. Three components:\n\n1. **Pass / Mixed / Fail** — your overall call.\n2. **Evidence** — one or two specific things that pushed you toward that call (a quote from the trace, a value that came back, a step that didn't work).\n3. **Recommended action** — for Mixed/Fail, what to change. For Pass, nothing — just say it's installed and how to invoke it.\n\nExample of a good verdict:\n\n> **Pass.** Trace #3 returned `cheapest_direct_usd: 134.50` for the JFK→LAX route on the dates you asked for, and the new dates appeared in the URL exactly where they should have. The flow was 9 steps, same as the originals. Cookie banner from trace #1 didn't appear in trace #3 — Booking.com probably remembers consent across sessions in stealth mode, no action needed. Skill is installed at `~/.claude/skills/flight-price-probe/`. Invoke with the four parameters listed in its description.\n\nExample of a useful Mixed verdict:\n\n> **Mixed.** Trace #3 reached the results page and read a price (`$220.00`), but the price is for a 1-stop flight, not direct. The selector for the \"Direct only\" radio button didn't engage in trace #3 — looks like the button moved positions and the skill clicked the wrong row. Recommend revising the click step to identify the radio by its accessible name (\"Direct only\") rather than by position. Want me to revise and rerun?\n\nBe concise. The user doesn't want a graded essay — they want to know if the skill works and what to do next.\n" },
    { path: "steel-skill-creator/references/parameterization.md", text: "# Parameterization\n\nUse two successful traces to separate variables from invariants.\n\n## Parameter Signals\n\n- URL query values differ at the same conceptual step.\n- Form input values differ but field identity stays the same.\n- Clicked result labels differ because the input changed.\n- Date ranges, IDs, names, routes, locations, or search terms change.\n\n## Invariant Signals\n\n- start URL or domain remains the same\n- same form labels and buttons are used\n- same wait points appear\n- same success signal appears\n- same auth/profile requirements apply\n\n## Naming\n\nName parameters by meaning:\n\n- `origin_airport`, not `param_1`\n- `report_date`, not `date_input`\n- `customer_id`, not `id`\n\nDo not include session IDs, trace IDs, timestamps, or incidental scroll offsets as parameters unless the user explicitly requires them.\n" },
    { path: "steel-skill-creator/references/skill-template-guide.md", text: "# Skill Template Guide\n\nUse `templates/browser-task-skill/` for generated browser task skills.\n\n## Required Sections\n\n- frontmatter with `name` and trigger-focused `description`\n- purpose and boundary\n- inputs\n- prerequisites\n- workflow\n- success criteria\n- verification\n- troubleshooting handoffs\n- references\n\n## Description Rules\n\n- Third person.\n- Under 1024 characters.\n- Includes what the skill does.\n- Includes when to use it.\n- Includes negative routing when overlap is likely.\n\n## Body Rules\n\n- Prefer 40-80 lines for generated task skills.\n- Use semantic steps over brittle CSS selectors when possible.\n- Keep references one level deep.\n- Do not embed raw traces, session IDs, timestamps, or credentials.\n" },
    { path: "steel-skill-creator/references/skill-template.md", text: "# Generated skill template\n\nUse this scaffold when authoring the SKILL.md for the skill you're generating. Fill in every section; delete anything you don't need rather than leaving placeholder text. Aim for 60–120 lines of body. The trace is the source code; the SKILL.md is the abstract — and the abstract leans on the `steel-browser` skill for the actual primitives.\n\n## Generated skills depend on `steel-browser`\n\nEvery generated skill assumes the executor has the `steel` CLI installed and the `steel-browser` skill loaded. The executor invokes `steel-browser` to drive the actual browser; the generated skill describes *what* to do in plain intent, not *how* to do it command by command.\n\nThis is the right shape because:\n\n- The steel CLI and its primitive names (`snapshot`, `click @eN`, `fill`, `wait`, `batch`, `eval`, `stop`) live in one place. Changes there flow to all generated skills automatically.\n- A generated skill stays terse and readable — what the *abstract* should be.\n- Onboarding for a new user is one curl command: `curl -fsS https://setup.steel.dev | sh`. After that, anything generated by `steel-skill-creator` runs.\n- The executor's intelligence is the runtime. Telling it \"click the *Where from?* combobox and fill it with `{origin}`\" is enough; it knows from `steel-browser` how to express that as a `snapshot → find @eN → click → fill → wait` sequence.\n\nEvery generated skill must include a `## Prerequisites` section that states this dependency and points at the install command. See the worked example below for the exact wording.\n\n## Frontmatter\n\n```yaml\n---\nname: <kebab-case-name>\ndescription: <one sentence on what the skill does, followed by a sentence on when to trigger it. Be a little pushy on triggering — Claude tends to under-trigger skills. Include the specific phrases/contexts a user would say when this skill applies. End with \"Requires the `steel-browser` skill (install via `curl -fsS https://setup.steel.dev | sh`).\" so future Claudes know the dependency.>\n---\n```\n\n## Body structure\n\n```markdown\n# <Skill display name>\n\n<One short paragraph on what this skill is for. Lead with the outcome.>\n\n## Prerequisites\n\nThis skill drives a real browser via Steel. If you don't already have Steel set up:\n\n```bash\ncurl -fsS https://setup.steel.dev | sh\n```\n\nThat installs the `steel` CLI and the `steel-browser` skill into `~/.claude/skills/`. Then export `STEEL_API_KEY` (get one at app.steel.dev/settings/api-keys). If `steel-browser` wasn't loaded when Claude Code started, restart so it picks up the new skill, then invoke this skill again.\n\nThis skill assumes `steel-browser` is available and uses its primitives.\n\n## Inputs\n\n- `<param_name>` — <what it is, format expected, example value>\n- `<param_name>` — <...>\n\n<If the skill has additional preconditions the executor cannot satisfy themselves — Steel credentials in the vault for a specific origin, a profile that must exist, a paid Steel plan feature — list them in a \"Site-specific preconditions\" subsection.>\n\n## Steps\n\n1. **Start a Steel session.** <Describe the configuration the session needs: stealth on if the site is bot-heavy, proxy region if geo-sensitive, credentials/profile if logged-in state is needed.>\n\n2. **<Phase name>.** <Terse intent prose. Name elements by accessibleName. State waits explicitly.>\n\n...\n\nN. **Stop the session.**\n\n## Output\n\n<What the skill returns. Be concrete: a JSON shape, a file path, a price. Include an example.>\n\n## Notes & caveats\n\n<Real-world failure modes you observed during the two recording runs. This is operator wisdom — don't leave it out.>\n```\n\n## Style guidance for the body\n\n**Lean on `steel-browser` primitives.** The executor knows them. You write \"click the *Where from?* combobox and fill it with `{origin}`\"; the executor turns that into the snapshot-find-click-fill sequence. Don't expand it for them — it's noise.\n\n**Refer to elements by their accessibleName, in italics.** \"Click the *Done* button\" beats \"click the button at the top of the date picker\". The accessibleName comes straight from the trace and is what the executor will look for during snapshot.\n\n**State waits as part of the step that needs them.** \"Wait for the suggestion list to appear, then pick the first option whose accessibleName ends with `(IATA)`.\" Not a separate \"wait for networkidle\" step floating in the middle.\n\n**Imperative voice.** \"Click X.\" Not \"The skill should click X.\"\n\n**Templated URLs beat click sequences.** If a user input ends up in the URL (`?from=SPU&to=FCO`), navigate to the templated URL directly. Skip the intermediate clicks. Every click skipped is one less brittle DOM dependency.\n\n**Use `eval` patterns for bulk reads or stable-attribute clicks.** If the step is \"read the aria-label of every gridcell and parse them\", describe that intent and the executor knows to reach for `steel browser eval`. Same for \"click the gridcell whose aria-label is X\" — describe the intent, not the eval syntax.\n\n**Lead with intent, not mechanism.** \"Apply the nonstop filter\" beats \"click the *Stops* filter button, then click the *Nonstop only* radio, then click *Close dialog*\". You can mention the elements involved as a sub-bullet if needed, but the top-line is intent.\n\n**Don't include the original trace's parameter values.** If trace #1 was SPU→FCO and trace #2 was JFK→LAX, the generated skill takes `origin` and `destination`, not \"SPU\" or \"JFK\". Hardcoding either is a tell that you didn't parameterize.\n\n**Don't include trace IDs, session IDs from your recording runs, or timestamps.** Those are compilation artifacts; they don't belong in the reusable skill.\n\n## Worked example (excerpt)\n\nThis is roughly what the steps look like for the `cheapest-direct-flight` skill. Note the intent-first phrasing, italicized accessibleNames, named primitives without expanded commands:\n\n```markdown\n## Steps\n\n1. **Start a stealth Steel session.** Google triggers bot challenges intermittently, so default stealth on. 10-minute timeout is enough for a full flow.\n\n2. **Open Google Flights with USD pinned.** Navigate to `https://www.google.com/travel/flights?hl=en&curr=USD`, wait for `networkidle`.\n\n3. **Set origin.** Click the *Where from?* combobox, fill it with `{origin}`, wait for the suggestion list, pick the first option whose accessibleName ends with `(IATA)`. If the field shows stale text, send `Control+A` then `Backspace` before filling.\n\n4. **Set destination.** Same pattern with the *Where to?* combobox and `{destination}`.\n\n5. **Set departure date.** Open the date picker by clicking the *Departure* textbox. Click the gridcell whose aria-label matches `{depart_date}` formatted as \"Weekday, Month D, YYYY\". Click any later gridcell as a throwaway return (the Date grid in step 8 overrides it). Click the *Done* button. If refs churn between snapshots in the picker, fall back to clicking by aria-label via `eval`.\n\n6. **Run the search.** Click *Search*, wait until the URL contains `/travel/flights/search` and the text \"round trip\" appears.\n\n7. **Apply the Nonstop filter.** Open the *Stops* filter, pick *Nonstop only*, close the dialog, wait for `networkidle`.\n\n8. **Open the Date grid.** Click *Date grid*, wait for `networkidle`.\n\n9. **Read all cells and pick the cheapest.** Read every gridcell's aria-label in one `eval` — each is shaped like `\"$NNN, MMM D to MMM D\"`. Parse into `(price, depart, return)`, filter to the requested `flex_days` and `min/max_nights` window, pick the minimum. If zero match, wait 3s and re-read once (the grid lags the filter state); still zero, return `null`.\n\n10. **Confirm the pick.** Click the winning gridcell by aria-label, click *OK*, wait for `networkidle`. If \"Oops, something went wrong\" renders, click *Reload* once; if it fails twice, abort.\n\n11. **Read the first flight.** Find the *Top departing flights* heading (fall back to *Other departing flights*), read the first listed flight's text. Parse out airline, times, price.\n\n12. **Stop the session.**\n```\n\nThat's the whole shape — 12 numbered steps, each one a sentence or two of intent, no expanded CLI commands. The executor reads this and runs it through `steel-browser`.\n" },
    { path: "steel-skill-creator/references/steel-primitives.md", text: "# Choosing the right Steel primitives for the generated skill\n\nUse this decision tree when authoring the generated skill's session-creation step. Match what you saw in the trace to the right Steel feature; do not over-configure (every extra option is a thing that can break or cost money) and do not under-configure (a skill that breaks the first time a real CAPTCHA appears is worse than no skill).\n\n## Quick reference\n\n| Signal | Steel primitive | Plan |\n|---|---|---|\n| Target domain is on the known-bot-heavy list (see below) | `--stealth` / `use_proxy: true` (default on) | Paid |\n| Password input on a login form | Credentials vault | All tiers |\n| Site requires persistent logged-in state across sessions | Profile | All tiers |\n| URL contains `chal_t=`, redirect to `/sorry/`, or your trace #1 needed `--stealth` to complete | `use_proxy: true` | Paid |\n| reCAPTCHA / hCaptcha / Cloudflare turnstile iframe seen in either trace | `solve_captcha: true` | Paid |\n| Content depended on geographic location, or you pinned `region: \"lax\"` / `region: \"iad\"` during recording | `useProxy: { geolocation: { country: ... } }` | Paid |\n\n## Known bot-heavy sites — default stealth on\n\nSome domains have aggressive bot detection that blocks or rate-limits non-stealth traffic intermittently, even when no specific challenge appears in the trace. If the target site is one of these (or a subdomain of one), default to `--stealth` / `use_proxy: true` from the first run, **before** looking for trace signals. This is cheap insurance — the false positive cost (paying for a proxy on a site that didn't strictly need it) is low; the false negative cost (a skill that works once and then mysteriously fails in production) is high.\n\nDefault-stealth domains include:\n\n- `google.com`, `google.<TLD>` — Search, Maps, Flights, Scholar; `/sorry/` redirect is common\n- `facebook.com`, `instagram.com`, `whatsapp.com`, anything under `*.meta.com`\n- `linkedin.com` — aggressive bot detection\n- `twitter.com`, `x.com`\n- `booking.com`, `expedia.com`, `kayak.com`, `airbnb.com`, `vrbo.com` — travel aggregators all check\n- `ticketmaster.com`, `livenation.com`, `stubhub.com`, `seatgeek.com` — ticketing sites are some of the strictest\n- `amazon.com`, `amazon.<TLD>` — variable but often challenges\n- `reddit.com` — anonymous browsing increasingly gated\n- `cloudflare.com`-protected sites (you can detect this by checking `Server: cloudflare` headers or seeing a `__cf_bm` cookie set on the first navigation)\n- Any banking, brokerage, or financial site\n\nThis list is not exhaustive. The rule of thumb: if the site shows logged-in-user content, sells something, or aggregates third-party data, assume bot detection. If it's a static documentation site, a Wikipedia mirror, or a government info page, stealth is unnecessary.\n\nWhen you enable stealth proactively, mention it in the generated skill's first step (\"Stealth on by default — Google triggers bot challenges intermittently\") so the user understands why the option is there.\n\n## Credentials vault\n\nUse this when the trace shows a login form interaction.\n\n**Setup (one-time, runs before the generated skill is first used):**\n```bash\nsteel credentials create --origin https://example.com --username \"<user>\" --password \"<pass>\"\n```\n\n**In the generated skill's session-start step:**\n```bash\nsteel browser start --session \"$SESSION\" --credentials\n```\n\nSteel detects the login form on the page and auto-fills from the vault. The generated skill never sees the credentials in plaintext.\n\n**What to write in the generated SKILL.md (as a site-specific precondition):**\n> Before first use, run `steel credentials create --origin https://example.com` with your username and password. Steel stores them in its vault and fills them automatically when this skill encounters the login form.\n\nAlways emit the precondition. Never put credentials in the skill body, parameters, or environment variables. If the user has explicitly asked for credentials-as-parameters (e.g., for multi-account workflows), accept it but warn about the risk.\n\n## Profiles\n\nUse this when the trace shows the user was already logged in but you don't see the login flow itself (meaning the session started authenticated). This typically means a profile or imported cookies was in play.\n\n**Setup (one-time):** create a named profile via the Steel CLI or SDK, drive a session into a logged-in state once, then save it.\n\n**In the generated skill's session-start step:**\n```bash\nsteel browser start --session \"$SESSION\" --profile \"${profile_id}\"\n```\n\nThe generated skill should accept `profile_id` as an input parameter — different users will have different profiles. Do not hardcode it.\n\nIf the steel CLI doesn't expose a profile flag in your installed version, the generated skill should fall back to the SDK form and the body should note the CLI/SDK split.\n\n## Stealth / proxy\n\nTrigger conditions, in priority order:\n0. The target domain is on the known-bot-heavy list above. Enable stealth from the first recording. This is the most important rule — observe it *before* you try a clearnet recording. A failed first run wastes a session; a stealth-on first run almost always works.\n1. Trace #1 needed `--stealth` to complete (you tried without and got challenged; you'll know because you debugged it yourself).\n2. Either trace contains a navigation whose URL has `chal_t=` (Booking.com challenge token), `/sorry/` (Google), `cf-chl-` (Cloudflare challenge), or similar bot-detection markers.\n3. Either trace contains visible references to bot challenge iframes or pages.\n\nWhen any of these are true, the generated skill's session-start step should look like:\n```bash\nsteel browser start --session \"$SESSION\" --stealth --session-timeout 600000\n```\n\n`--stealth` routes through Steel's residential proxy pool and enables anti-detection by default.\n\nOptionally pin a region if the trace showed geo-sensitive content (a country-specific TLD, a localized currency that doesn't match the user's clearnet location):\n```bash\nsteel browser start --session \"$SESSION\" --stealth --proxy-country DE\n```\n\nOr pick a Steel edge region for latency:\n```bash\nsteel browser start --session \"$SESSION\" --stealth --region iad   # or lax\n```\n\nIf the installed steel CLI doesn't accept those flags, the generated skill body should reach for the SDK equivalent and note the fallback.\n\n**Cost note for the generated SKILL.md:** Mention that stealth/proxy is a paid Steel feature so the user isn't surprised by billing. One line is enough; don't lecture.\n\n## CAPTCHA solving\n\nTrigger conditions:\n1. You needed `solve_captcha: true` to get either trace to complete.\n2. Either trace shows interaction with a CAPTCHA iframe (reCAPTCHA, hCaptcha, Cloudflare turnstile) — search the trace for `iframe` targets with `src` matching these.\n3. The flow hit a `chal_t=` URL and required a click-through.\n\n`--stealth` already includes automatic CAPTCHA solving on paid plans — no separate flag needed. Steel detects reCAPTCHA / hCaptcha / Cloudflare turnstile and solves silently while the skill is running.\n\nIf the skill needs to explicitly check or trigger CAPTCHA handling (rare; only when the flow has unusual timing around the challenge), use the `steel-browser` CLI's `captcha status --wait` and `captcha solve` commands in-step:\n\n```bash\nsteel browser captcha status --wait --session \"$SESSION\"\nsteel browser captcha solve --session \"$SESSION\"\n```\n\nOnly emit manual mode if the trace shows the user did something custom around the CAPTCHA. Otherwise, default to `--stealth` and let it happen.\n\n## Putting it together\n\nA typical generated skill's first step should look like one of these shapes:\n\n**Simple unauthenticated, low-bot-detection flow:**\n```bash\nsteel browser start --session \"$SESSION\" --session-timeout 600000\n```\n\n**Logged-in flow on a bot-heavy site:**\n```bash\nsteel browser start --session \"$SESSION\" --stealth --credentials --session-timeout 600000\n```\n\n**Pre-authenticated via profile:**\n```bash\nsteel browser start --session \"$SESSION\" --stealth --profile \"${profile_id}\" --session-timeout 600000\n```\n\nPick the smallest set of options that matches the evidence. If you find yourself adding `--stealth` \"just in case\" on a site that isn't on the bot-heavy list and never challenged either trace, drop it. Conversely, if the site *is* on the bot-heavy list, stealth on regardless of whether a challenge appeared.\n" },
    { path: "steel-skill-creator/references/trace-anatomy.md", text: "# Reading a Steel agent trace\n\nUse this when you have trace JSON from `steel sessions traces <session-id> --json` or `~/.claude/skills/steel-skill-creator/scripts/fetch_trace.mjs` and need to extract meaning from it.\n\n## What the trace contains\n\nThe trace JSON has the shape:\n\n```json\n{\n  \"session_id\": \"...\",\n  \"event_count\": 24,\n  \"events\": [ /* AgentActivity records */ ]\n}\n```\n\nEach event is one *coalesced* unit of user activity. Coalescing means: ten keystrokes become one `input(len=10)`, two rapid clicks become one `dblclick`, a redirect chain becomes a series of `navigate` events (one per distinct URL).\n\n## The fields that actually matter\n\nFor each event, look at these first — in order:\n\n1. **`type`** — `click`, `dblclick`, `input`, `keyPress`, `navigate`, `scroll`, `drag`, `submit`, `error`. This tells you what kind of action it is.\n2. **`target.accessibleName`** — the human-readable label of the element (button text, input label, link text). **This is the single most important field.** It is what you should use when writing the generated skill's steps. \"Click the *Direct flights only* checkbox\" is a thousand times more durable than `click div:nth-of-type(3) > input`.\n3. **`page.url`** — the URL the event happened on. Group consecutive events by URL to identify page boundaries.\n4. **`target.attributes`** — `id`, `data-testid`, `aria-label`, `name`, `href`, `placeholder`, etc. Useful when `accessibleName` is empty or generic.\n5. **`target.selector`** — a prioritized list: `testId`, `id`, `aria`, `name`, `css`, `xpath`. Use this as evidence for available selectors. When writing generated skill steps, prefer a specific accessible name or visible text; if that is missing or too generic, walk this selector list top to bottom and pick the first intentional, stable selector.\n6. **`value`** — present on `input`, `keyPress`, `submit`. For inputs, the field type and length (sensitive values are replaced with `<redacted>`). This is where you identify parameters that came from the user typing.\n7. **`pointer.x`, `pointer.y`** and **`target.boundingBox`** — coordinates. Useful for sanity checks and for grid-like UIs (e.g., tic-tac-toe cells) where geometry *is* the semantics. Otherwise, prefer the accessibleName/selector path.\n\n## Signals you should care about\n\n### Page boundaries\nA change in `page.url` between consecutive events is a page transition. In the markdown export this is a `##` heading. Use these to chunk the trace into *phases* of the flow.\n\n### Idle gaps\nIf consecutive events are far apart in time (you'll see `*(idle Xs)*` in the markdown export, or a large delta in the JSON timestamps), the activity *after* the gap is something that had to wait for the page to do something. This is where the generated skill needs `wait --load networkidle`, `wait -t \"<text>\"`, or `wait --selector`. Use the next event's target to decide which wait to emit:\n- Next event interacts with an element identified by visible text → `wait -t \"<that text>\"`.\n- Next event interacts with an element identified by a structural selector → `wait --selector \"<that selector>\"`.\n- Next event is just a navigation following a click → `wait --load networkidle`.\n\n### Pointer-at-(0,0) clicks\nThese are eval-driven clicks (the script called `element.click()` rather than dispatching a synthetic mouse event). They are valid clicks — they just don't carry real coordinates. Use the selector or accessibleName, ignore the (0,0).\n\n### Zero-displacement drags\nA `drag` with identical start and end coordinates is almost certainly a misclassified click. Treat it as a click on the target. This is a known coalescing artifact (we encountered it in the tic-tac-toe trace).\n\n### `error` events\nPage-level JavaScript errors logged by the page. Worth scanning — if an error appears next to a step in the trace, the original flow may have been semi-broken even if it appeared to succeed. Flag for the user.\n\n### Login flows\nLook for `input` events on fields where the attribute set includes `type=password`, `autocomplete=current-password|new-password`, or names like `password`, `passwd`, `pwd`. When you see one, do not parameterize the credentials in the generated skill — see `steel-primitives.md`, use the credentials vault.\n\n## Reading two traces in parallel for parameter extraction\n\nYou will be reading trace #1 (your first run, with the user's example inputs) and trace #2 (your second run, with mutated inputs) together. The mechanic:\n\n1. Align the two by event index, starting from the first navigation each.\n2. Walk forward. For each pair of corresponding events:\n   - If `type` and `target` shape are the same and the `value` or URL query string differs → this is a **parameter**. Note its position, the two observed values, and what kind of field/URL it was.\n   - If both are identical → **invariant**. The generated skill will hardcode it.\n   - If structurally different (different element type, different page) → the flows diverged. Stop and investigate. Maybe the AI showed a modal in one run and not the other; maybe the page restructured. Decide whether the divergence matters before continuing.\n\n3. When you have a set of parameters, give them names based on what they *mean* on the page where they appear — read the surrounding labels, the URL path, the form's purpose. `depart_date`, not `var_2`.\n\n## Things that look like data but probably aren't parameters\n\n- Pointer coordinates (unless the UI is a coordinate grid like a game board).\n- Scroll positions.\n- Tracking-shaped URL params: `chal_t=`, `gclid=`, `sei=`, `aid=`, `label=`, `force_referer=`. These are noise. Strip them.\n- Timestamps in URLs.\n- Cookie banner / modal-dismiss clicks that only appeared in one trace. Treat as opportunistic handlers, not parameters.\n\n## When the trace tells you the source URL pattern\n\nURL query strings are gold for parameterization. If trace #1 has `?from=SPU&to=FCO&depart=2026-06-13` and trace #2 has `?from=JFK&to=LAX&depart=2026-07-04`, the generated skill's navigation step becomes a templated URL — no clicks needed at all for that part of the flow. Always prefer templated URLs over click sequences when both achieve the same state. Fewer DOM dependencies = a more durable skill.\n" },
    { path: "steel-skill-creator/references/verification.md", text: "# Verification\n\nVerify generated skills with a third input set that differs from both recording runs.\n\n## Verification Steps\n\n1. Install the generated skill locally.\n2. Start a fresh agent context or ensure the skill can be discovered.\n3. Invoke the skill with new inputs.\n4. Run the browser task through Steel.\n5. Fetch the third trace.\n6. Compare outcome fidelity against the first two traces.\n\n## Verdict\n\nUse one of:\n\n- Good: third run reached the same success signal with clean parameter substitution.\n- Mixed: success was partial or required manual recovery.\n- Failed: the skill did not trigger, selected wrong steps, or missed the success state.\n\nReport caveats plainly and revise the generated skill before calling it ready.\n" },
    { path: "steel-skill-creator/templates/browser-task-skill/README.md", text: "# __SKILL_NAME__\n\nGenerated browser task skill.\n\n## Install\n\n```bash\nnpx skills add .\n```\n\n## Example Prompts\n\n- \"Run `__SKILL_NAME__` with <inputs>.\"\n\n## Files\n\n- `SKILL.md`: main reusable instructions.\n- `references/`: workflow, examples, and troubleshooting.\n- `evals/evals.json`: generated skill evals.\n" },
    { path: "steel-skill-creator/templates/browser-task-skill/SKILL.md", text: "---\nname: __SKILL_NAME__\ndescription: '__DESCRIPTION__'\nlicense: MIT\ncompatibility: claude-code,codex,opencode\nmetadata:\n  owner: steel\n  category: browser-task\n  stage: experimental\n---\n\n# __SKILL_NAME__\n\n## Purpose\n\nRun a recurring browser workflow with Steel using user-provided inputs.\n\n## Inputs\n\n- Replace this list with the concrete parameters discovered from the two traces.\n\n## Prerequisites\n\n- Steel CLI is installed and authenticated.\n- The `steel-browser` skill is available for live browser execution.\n- Any required profiles or credentials have been configured before running.\n\n## Workflow\n\n1. Confirm required inputs.\n2. Start a named Steel browser session with only the options justified by trace evidence.\n3. Navigate to the starting URL.\n4. Follow the stable steps identified in the reference trace.\n5. Substitute user inputs only where parameters were identified.\n6. Verify the success signal.\n7. Stop or release the session unless the user asks to keep it open.\n\n## Success Criteria\n\n- The workflow reaches the same kind of final state as the recording traces.\n- The output is returned in the format the user requested.\n- No trace IDs, session IDs, timestamps, or secrets are included in reusable instructions.\n\n## References\n\n- `references/workflow.md`: task-specific detailed workflow.\n- `references/examples.md`: example inputs and outputs.\n- `references/troubleshooting.md`: known failure recovery.\n" },
    { path: "steel-skill-creator/templates/browser-task-skill/references/examples.md", text: "# Examples\n\n## Happy Path\n\nInput:\n\n```json\n{}\n```\n\nExpected output:\n\n```json\n{}\n```\n\nReplace with concrete examples from verification.\n" },
    { path: "steel-skill-creator/templates/browser-task-skill/references/troubleshooting.md", text: "# Troubleshooting\n\n- If the task fails before evidence exists, use `steel-session-debugging`.\n- If the failure is bot detection, CAPTCHA, proxy, profile identity, or pacing, use `steel-reliability`.\n- If the page shape changed, capture two new traces and update the workflow.\n- If a selector fails, prefer accessible names, labels, roles, or `data-testid` before raw CSS.\n" },
    { path: "steel-skill-creator/templates/browser-task-skill/references/workflow.md", text: "# Workflow\n\nReplace this file with the stable workflow steps from the two trace recordings.\n\nUse semantic instructions first:\n\n- Click the button by visible name.\n- Fill fields by label.\n- Wait for specific page state.\n- Extract the final result.\n\nUse raw selectors only when no stable semantic target exists.\n" },
];
